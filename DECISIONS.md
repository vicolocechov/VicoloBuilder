# Decision Log

Registro versionato delle decisioni architetturali rilevanti, con la loro motivazione, l'evidenza disponibile al momento della decisione, e quando vanno rivalutate. Non sostituisce le RFC (`PROJECT_BRIEF.md`) — le RFC definiscono i vincoli, questo file registra le scelte fatte dentro lo spazio che le RFC lasciano aperto.

Le voci D-001/D-002/D-003/D-004 sono un backfill delle decisioni prese durante la Fase 1 (Document + CommandBus + History), non ancora registrate in un log persistente prima d'ora. Le voci D-005+ sono emerse durante l'analisi di apertura della Fase 2.

---

## D-001 — Immutabilità runtime (Document)

**Stato**: nessuna immutabilità runtime per ora (`Object.freeze` o equivalente non implementato). `readonly`/`ReadonlyMap` restano garanzie solo compile-time.

**Motivazione**: nessun consumer esterno esiste ancora che possa violare "Command Bus unico punto di scrittura" (RFC-000 §3) bypassando `applyCommand` — il CLI è Fase 3, il renderer-react è Fase 5. L'Engine stesso non muta mai il proprio input (verificato via grep diretto sugli import/pattern vietati + test di purezza).

**Evidenza disponibile**: analizzate 4 opzioni indipendenti (nessuna / freeze solo dev / freeze completo / freeze solo test harness). Un freeze completo naive (l'intero Document risultante, non solo gli oggetti nuovi) misura +2,8ms per comando a N=10.000 nodi (+40% sopra il costo base di ~6,5ms), riducendo il margine sulla soglia di 16ms da 2,4× a ~1,7×. L'opzione "freeze solo in development" è risultata tecnicamente in conflitto con `purity.test.ts` (che vieta la lettura di `process.env` dentro `packages/engine/src`, coerente con RFC-000 §2/§10) a meno di essere implementata come opt-in esplicito lato-consumer. Traccia scritta anche in `packages/engine/src/document/types.ts` (commento sopra l'interfaccia `Document`).

**Rivalutazione**: esplicitamente all'inizio della Fase 3, con l'introduzione del CLI (primo consumer esterno reale).

---

## D-002 — Cold-start di `execute()`

**Stato**: non è un problema dimostrato per la Fase 2.

**Motivazione**: Resolver e LayoutEngine, per come descritti in RFC-003/RFC-004, non chiamano `execute()`/`applyCommand` — leggono un Document già esistente e producono un output derivato, senza passare dal CommandBus. Il dato misurato riguarda un componente diverso (`applyCommand`), non trasferibile 1:1 a codice non ancora scritto.

**Evidenza disponibile**: la primissima chiamata di processo ad `applyCommand` contro un Document da 10.000 nodi costa ~16-38ms, contro ~5-8ms dalla seconda/terza chiamata sulla stessa "forma" di dato. **Correzione rispetto alla formulazione iniziale**: l'attribuzione di questo costo a "JIT/inline-cache di V8" non è stata isolata sperimentalmente (nessun uso di `--jitless`/`--trace-opt`/`--trace-deopt`) — è un'inferenza plausibile basata sull'osservazione di un pattern di convergenza su chiamate ripetute, non una causa dimostrata. La conclusione operativa (non è un problema per Fase 2) non dipende dalla causa esatta, solo dal fatto che il componente misurato (`applyCommand`) è diverso da quello che la Fase 2 introdurrà.

**Rivalutazione**: quando esisterà un vero path di caricamento/import di un documento salvato (Fase 3, CLI) — lì la domanda diventa misurabile con dati reali invece che per analogia.

---

## D-003 — Costo O(n) delle mutazioni del Document (`new Map(document.nodes)`)

**Stato**: nessuna riscrittura della struttura dati; il costo O(n) per comando (O(n²) cumulativo su sequenze lunghe) è accettato e documentato, non ottimizzato.

**Motivazione**: misurato che il costo marginale di un singolo comando (`CREATE_NODE`/`UPDATE_PROPS`/`DELETE_NODE`) a N=10.000 nodi resta entro tutti i budget di RFC-000 §6 con margine ≥1,9× anche sul più stretto (16ms). Il costo cumulativo O(n²) (fino a ~27s per costruire 10.000 nodi via comandi sequenziali) esiste ma non corrisponde a nessun requisito esplicito di RFC-000 (nessuna sezione della RFC, riletta per intero, parla di throughput/bulk-import/limiti temporali cumulativi). Riscrivere ora la struttura dati (es. persistent map/HAMT) sarebbe ottimizzazione senza una violazione misurata da correggere.

**Evidenza disponibile**: benchmark isolati per comando a 1.000/2.000/4.000/8.000/10.000 nodi (Fase 1, Priorità 1.1); scomposizione della cascata di `DELETE_NODE` che esclude `collectSubtreeIds` come causa (cancellare un sottoalbero da 5.000 nodi costa ~7,9ms contro ~6,0ms per uno da 10 nodi, a documento totale fisso). Commento in-code su tutti e 3 i siti (`packages/engine/src/runtime/commands.ts`, righe 68/87/103 circa).

**Rivalutazione**: se il target nodi cresce molto oltre 10.000, oppure se viene introdotta un'operazione di bulk-edit che emette molti comandi senza pause di rendering.

---

## D-004 — Metodologia dei benchmark di performance

**Stato**: ogni benchmark su operazioni sub-16ms misura la MEDIANA su più ripetizioni dopo un warm-up esplicito, mai un singolo campione a soglia temporale diretta.

**Motivazione**: un design a singola misura si è dimostrato flaky in questo ambiente sandboxato (fino a ~21ms su singolo campione contro una mediana reale di 5-6ms), per rumore dello scheduler/worker-thread di vitest — non per un difetto del codice misurato (uno script Node standalone con lo stesso identico warm-up è rimasto stabile sotto 8,3ms su 30 campioni).

**Evidenza disponibile**: `packages/engine/test/performance.test.ts` — 15 esecuzioni consecutive verdi dopo l'adozione di mediana-su-15-ripetizioni, contro un tasso di fallimento di circa 2/5 con singolo campione anche dopo warm-up generico.

**Rivalutazione**: non prevista salvo emerga nuova flakiness; in quel caso il primo intervento è aumentare il numero di ripetizioni misurate, non aggiungere altro warm-up non giustificato da una misura.

---

## D-005 — Immutabilità runtime estesa a ResolvedModel/Box Tree (Fase 2)

**Stato**: nessuna immutabilità runtime, stessa conclusione di D-001, estesa qui con motivazione propria.

**Motivazione**: vale anche qui perché nessun consumer esterno esiste ancora che possa mutare accidentalmente un `ResolvedModel` o un Box Tree — la stessa condizione fattuale di D-001 (CLI = Fase 3, renderer-react = Fase 5), non un'eredità automatica della conclusione di D-001. La premessa è verificata indipendentemente: né RFC-003 né RFC-004 menzionano consumer di Resolver/Layout prima della Fase 5.

**Evidenza disponibile**: nessuna misura specifica su ResolvedModel/Box Tree (non esistono ancora); il ragionamento è per analogia sulla premessa (assenza di consumer), non sul costo (il costo di un eventuale freeze su una struttura diversa da Document non è stato misurato).

**Rivalutazione**: stessa di D-001 — inizio Fase 3.

---

## D-006 — Nessuna soglia numerica fissata ora per il costo combinato Resolver+Layout

**Stato**: non viene fissata una soglia in millisecondi per Resolver+Layout in questa fase di analisi.

**Motivazione**: RFC-000 §6 dà un solo numero complessivo ("export preview <100ms") che copre concettualmente Resolver+Layout+Exporter insieme; Exporter non esiste ancora e non è chiaro quanto budget si riserverà. Fissare ora una frazione arbitraria di quel numero per Resolver+Layout ripeterebbe esattamente l'errore che l'utente ha chiesto di evitare in Fase 1 (soglie non motivate da un dato).

**Evidenza disponibile**: nessuna — è l'assenza di dato a motivare l'assenza di soglia.

**Rivalutazione**: quando si scriverà il benchmark reale in fase di implementazione di Fase 2, o quando l'Exporter avrà una forma concreta che permetta di ripartire il budget.

---

## D-007 — Layout incrementale vs completo

**Stato**: LayoutEngine ricalcola sempre l'intero Box Tree da zero (nessun ricalcolo incrementale/parziale) per ora.

**Motivazione**: stesso schema già usato in D-003 per il costo O(n) del `Map`-copy del Document — si accetta la soluzione più semplice e corretta finché un benchmark reale non dimostra una violazione, invece di introdurre in anticipo la complessità di un ricalcolo incrementale (short-circuit sulle sole porzioni del Document cambiate da un comando) senza un dato che la giustifichi.

**Evidenza disponibile**: nessuna — LayoutEngine non esiste ancora, il costo di Resolver+Layout a scala target (proprietà 12 della matrice di apertura Fase 2) non è stato ancora misurato.

**Rivalutazione**: quando il benchmark della proprietà 12 (una volta implementato) mostrerà che il ricalcolo completo viola il budget assegnato a Resolver+Layout — budget la cui soglia numerica non è ancora stata fissata (D-006). Non prima, per evitare di introdurre la complessità dell'incrementale senza una prova che serva.

---

## D-008 — Debito verso RFC-002 (tabella incorporata invece del Capability/Property Registry)

**Stato**: il Resolver di Fase 2 usa un mapping dichiarativo semplice incorporato (`variantTable.ts`), non il Capability Registry + Property Registry completo descritto in RFC-002.

**Motivazione**: coerente con lo scope "Resolver base" del piano operativo di Fase 2 (righe 129-136 del brief), che non menziona un registro — solo "risoluzione breakpoint + variant semantico". Costruire il registro completo ora sarebbe anticipare lavoro di RFC-002 non richiesto dal vertical slice, con il rischio di indovinare una forma sbagliata prima di avere un secondo caso d'uso reale che la vincoli.

**Evidenza disponibile**: nessuna misura — è una scelta di scope, non di prestazioni. Costo di migrazione noto e accettato: quando esisterà il registro completo, andrà toccato ciò che oggi consuma la tabella incorporata (`resolver/resolveNode.ts`).

**Rivalutazione**: quando/se si costruirà il registro completo (RFC-002, fuori dallo scope della Fase 2 del vertical slice).

---

## D-009 — Convenzione `props.responsive` per gli override per breakpoint

**Stato**: un nodo rappresenta gli override responsive con una proprietà riservata `props.responsive = { <breakpointName>: { ...override } }` (es. `props.responsive = { desktop: { padding: 24 } }`). È il meccanismo dichiarativo incorporato usato dal Resolver di Fase 2 per applicare la cascata mobile-first tra breakpoint (`resolver/resolveNode.ts`, `applyBreakpointOverrides`).

**Motivazione**: il Resolver deve poter risolvere "breakpoint" (Fase 2, riga 131 del piano operativo) contro un Document i cui nodi hanno un'unica `props: Record<string, unknown>` piatta (RFC-002/Fase 1) — senza un campo dedicato nel modello dati per gli override, serve una convenzione su come questi vengono rappresentati dentro `props` stessa. `responsive` è la chiave scelta, consumata durante la risoluzione e mai presente in `resolvedProps` (l'output finale non la espone).

**Natura della decisione**: è una convenzione interna e minimale, non un Capability/Property Registry completo — nessuna validazione dichiarativa della forma degli override, nessun controllo che le chiavi dentro `responsive` siano breakpoint noti (un nome sconosciuto viene semplicemente ignorato, non segnalato). Stessa scelta di scope di D-008: sufficiente per il vertical slice, non la forma finale.

**Evidenza disponibile**: nessuna misura — è una scelta di rappresentazione dei dati, non di prestazioni. Testata in `test/resolver/breakpoints.test.ts` (cascata mobile-first, assenza della chiave `responsive` in `resolvedProps`, comportamento invariato per nodi senza override).

**Rivalutazione**: quando una fonte successiva (RFC-002 completo, o una revisione di RFC-003) definirà una forma diversa per rappresentare gli override responsive, o quando arriverà il Capability/Property Registry completo (D-008) che potrebbe rendere questa convenzione ridondante o sostituirla con una rappresentazione validata.

---

## D-010 — Superficie pubblica semver: `BREAKPOINTS`/`VARIANT_TABLE` restano dettagli interni

**Stato**: `BREAKPOINTS`, `getBreakpoint`, `cascadingBreakpoints` e `VARIANT_TABLE` non sono esportati da `packages/engine/src/index.ts` (la superficie pubblica versionata per semver, RFC-000 §9/§11). Restano importabili internamente dai moduli dell'Engine e dai test tramite i loro percorsi diretti (`resolver/breakpoints.js`, `resolver/variantTable.js`), invariati.

**Motivazione**: sono strutture dati esplicitamente provvisorie (D-008 per `VARIANT_TABLE`; la lista fissa di breakpoint condivide la stessa natura di scelta di scope minimale per il vertical slice) e non esiste oggi alcun consumer esterno reale che ne richieda l'esposizione — verificato: nessun file nella suite di test importa alcunché dal barrel `src/index.ts`, nemmeno per gli export di Fase 1. Legare a semver (RFC-000 §9: "breaking change = major bump") dati grezzi che si sa già dover cambiare forma esporrebbe l'Engine a un costo di rottura evitabile.

**Nota di simmetria con Fase 1**: a differenza di questi due, i validator/error type di Resolver e Layout (`validateResolvedModel`, `assertValidResolvedModel`, `ResolvedModelInvariantError`, `validateBox`, `assertValidBox`, `BoxInvariantError`, e i relativi tipi) **restano esportati**, per coerenza diretta con l'equivalente già stabilito in Fase 1 (`validateDocument`/`assertValidDocument`/`DocumentInvariantError` sono già Public API dalla Fase 1, senza riserva di provvisorietà) e perché RFC-000 assegna esplicitamente al Test Runner (Fase 4) il compito di verificare gli invariant dall'esterno del package.

**Evidenza disponibile**: nessuna misura — è una scelta di scope della Public API, non di prestazioni.

**Rivalutazione**: quando emergerà un consumer esterno reale (CLI, Fase 3; renderer-react, Fase 5; Test Runner, Fase 4) che necessiti di conoscere i nomi di breakpoint/variant validi dall'esterno del package, o quando una fonte architetturale (es. RFC-002 completo, o una revisione di RFC-003 sul meccanismo di plugin) definirà la forma definitiva di queste strutture. In quel momento la superficie esposta andrà progettata intenzionalmente (es. solo nomi validi, non l'intera struttura dati interna), non ricavata automaticamente da ciò che oggi esiste internamente.

---

## D-011 — Limite di profondità ricorsiva condiviso (Document, Resolver, Layout)

**Stato**: rischio strutturale registrato, nessuna correzione applicata in questo momento.

**Motivazione**: esistono quattro punti ricorsivi indipendenti che condividono la stessa vulnerabilità a `RangeError: Maximum call stack size exceeded` su alberi molto profondi, distribuiti tra Fase 1 e Fase 2: `document/invariants.ts` (`visit()` per il rilevamento cicli, Fase 1, invocato incondizionatamente da ogni `applyCommand`), `resolver/invariants.ts` (stessa struttura, Fase 2), `layout/computeLayout.ts` (`layoutNode()`, ricorsione di costruzione, Fase 2), `layout/invariants.ts` (`visit()` per il controllo dei bound, Fase 2). Nessuna fonte (RFC-000 §6, RFC-004, RFC-002) specifica che il target di 10.000 nodi debba reggere indipendentemente dalla forma dell'albero — il requisito è silente sulla struttura, non la esclude né la impone. Il vettore realistico per raggiungere le profondità coinvolte (migliaia di livelli) tramite normale interazione drag-and-drop è valutato molto basso (analisi di prodotto, non garanzia verificabile — il renderer-react di Fase 5 non esiste ancora); un vettore più concreto è la costruzione programmatica di un Document (CLI di Fase 3 che legge JSON arbitrario, o un'estensione futura del test property-based di Fase 1 oltre le 40 operazioni attuali).

**Evidenza disponibile**: misurato in processi Node isolati (non nello stesso processo, che altera la soglia osservata): `computeLayout` fallisce su una catena tra N=3500 (OK) e N=4000 (FAIL); `resolveDocument` tra N=5000 e N=5200; `validateDocument`/`assertValidDocument` (Fase 1) tra N=5000 e N=5500. Un albero bilanciato (branching=8) da 10.000 nodi — l'intera capacità target di RFC-000 §6 — non mostra alcun problema. La dimensione dello stack V8/Node non è specificata da ECMAScript ed è nota per essere dipendente dall'ambiente (OS, versione, flag di avvio, persino dallo stato del processo) — questi numeri non sono una costante universale.

**Rivalutazione**: verificato end-to-end (non solo per analogia) su `builder export`: un JSON esterno con catena a 6.000 nodi (sopra le soglie storiche di `validateDocument`, 5000-5500) produce un errore CLI pulito (exit 1, nessuno stack trace) grazie alla cattura del `RangeError` al confine CLI (Opzione 3, nessuna modifica ai 4 punti ricorsivi dell'Engine). La deserializzazione stessa (`document/deserialize.ts`) è iterativa per costruzione — nessun quinto punto introdotto, confermato anche nell'implementazione reale, non solo nell'analisi. Prossima rivalutazione: se in futuro si sceglierà un formato di caricamento non piatto, o se emergerà un vettore diverso dai 4+CLI già coperti.

---

## D-012 — Formato dell'IR ("Meta") e formato di deserializzazione JSON→Document

**Stato**: `Meta = { pageId, breakpoint }`; `exportIR(document, context): IR` con `context.breakpoint`, `context.pageId` e `context.viewportWidth` obbligatori ed espliciti (nessun default nascosto dentro l'Engine); Box Tree invariato da Fase 2 (`{nodeId,x,y,width,height,children}`); deserializzazione JSON→Document piatta/iterativa, simmetrica a `serializeDocument`; nessun `export/invariants.ts` dedicato per ora.

**Motivazione**: `pageId` e `breakpoint` sono richiesti da bisogni già presenti nel codice — `computeLayout` è già oggi single-page per costruzione (`options.pageId`, `packages/engine/src/layout/computeLayout.ts`), `ResolverContext.breakpoint` è già oggi un campo obbligatorio (`packages/engine/src/resolver/types.ts`) — non da usi futuri ipotetici, coerentemente con D-006/D-007. `viewportWidth` è stato aggiunto per lo stesso motivo dopo la stesura iniziale del piano: `ComputeLayoutOptions.viewportWidth` è già oggi un campo obbligatorio senza default in `computeLayout`, quindi `exportIR()` non può ometterlo. `pageName`/`schemaVersion` restano esclusi da Meta: nessuna funzione della pipeline li consuma oggi, sarebbero giustificati solo da un uso futuro ipotetico (stesso principio di D-006/D-007). Formato piatto per la deserializzazione preferito perché elimina strutturalmente (non solo mitiga) il rischio di un quinto punto ricorsivo rilevante per D-011 (nessuna fonte impone un formato annidato). Nessun validator dedicato perché `resolveDocument`/`computeLayout` validano già i propri output prima di restituirli, e Meta non introduce una struttura a grafo propria.

**Natura della decisione**: convenzione minimale per il vertical slice, non la forma finale/completa dell'IR prevista da RFC-005 — stessa natura di scelta-di-scope di D-008/D-009. Non copre i valori di default lato CLI per `breakpoint`/`viewportWidth` (`"desktop"`/`1280`, precedente informale dello script demo di Fase 2, non una relazione architetturale Engine): quella è una scelta UX del consumer CLI, documentata con un commento inline nel codice, non in questa voce.

**Evidenza disponibile**: nessuna misura di prestazioni — scelta di rappresentazione dati, verificata sul codice esistente (citazioni sopra).

**Rivalutazione**: quando un vero Exporter (HTML/React/Vue, fuori scope) rivelerà un bisogno concreto non coperto da questa Meta minimale, o quando RFC-005 verrà completata.

---

## D-013 — `Document.pageOrder` esplicito (Fase 5, Blocco A)

**Stato**: `Document` porta un campo `pageOrder: readonly PageId[]`, obbligatorio sul tipo in memoria, opzionale nel formato JSON esterno (`deserializeDocument` calcola un fallback alfabetico se assente). `serializeDocument` lo scrive preservando l'ordine reale (non riordinato alfabeticamente, a differenza di `pages`/`nodes`). Nuovi comandi `CREATE_PAGE`/`DELETE_PAGE`/`REORDER_PAGES` lo mantengono coerente (append in coda, rimozione, sostituzione completa via permutazione validata).

**Motivazione**: l'ordine di inserimento in una `Map` non sopravvive alla serializzazione (`serializeDocument` ordina `pages`/`nodes` alfabeticamente per id, di proposito, per il determinismo dell'hash — vedi `document/hash.ts`), esattamente lo stesso problema già risolto in Fase 1 per i figli di un nodo con `childrenIds`. Il riordino di pagine è un requisito esplicito di prodotto (PRODUCT_DESIGN.md, Decisione 4) che richiede un ordine osservabile e persistente, non solo un ordine implicito di iterazione.

**Natura della decisione**: `DELETE_PAGE` rifiuta di eliminare la pagina che coincide con `Document.rootPageId`, oltre a rifiutare l'eliminazione dell'ultima pagina rimasta — scelta conservativa emersa durante l'implementazione (non nel piano originale, segnalata a parte): nessun comando oggi permette di riassegnare `rootPageId`, quindi eliminarne la pagina lascerebbe il Document privo di una pagina predefinita valida (violazione `ROOT_PAGE_NOT_FOUND`) senza modo di ripararlo.

**Evidenza disponibile**: nessuna misura di prestazioni — scelta di rappresentazione dati, stessa natura di D-009. Verificato: rendere il campo obbligatorio sul tipo `Document` (non opzionale) richiede che ogni `Document` costruito a mano nei test esistenti lo includa — 3 file (`hash.test.ts`, `history.test.ts`, `performance.test.ts`) aggiornati di conseguenza; nessun consumer reale (`packages/cli`, `packages/test-runner`) costruisce `Document` a mano (solo `createDocument`/`deserializeDocument`), quindi nessuna rottura effettiva nonostante il campo sia obbligatorio e non opzionale sul tipo. Testato in `test/pages.test.ts` (12 test), estensioni a `test/invariants.test.ts` e `test/deserialize.test.ts`.

**Rivalutazione**: quando emergerà un comando per riassegnare `rootPageId` — a quel punto va deciso esplicitamente cosa succede a `DELETE_PAGE` sulla pagina che era radice.

---

## D-014 — `Box.mode` e condizionalità di `CHILD_OUT_OF_BOUNDS` (Fase 5, Blocco B)

**Stato**: `Box` porta un campo opzionale `mode?: "pila" | "libero"`, impostato da `layoutNode()` in base a `resolvedProps.layoutMode` del nodo stesso (non del genitore) — descrive come QUEL nodo dispone i propri figli. `layout/invariants.ts` applica `CHILD_OUT_OF_BOUNDS` solo se il genitore ha `mode !== "libero"` (quindi anche quando `mode` è assente, per compatibilità con l'algoritmo a pila preesistente).

**Motivazione**: tre assunzioni strutturali del LayoutEngine di Fase 2 (documentate in PRODUCT_DESIGN.md, sez. 6, decisione 3) bloccavano il posizionamento libero: il controllo dei bordi si applicava incondizionatamente a ogni livello; la larghezza era un parametro sempre ereditato top-down, mai letto dalle proprietà di un nodo; un contenitore non leggeva mai proprie dimensioni esplicite. Aggiungere un campo di modalità al `Box` (anziché, ad esempio, dedurre la modalità dalla sola presenza di `x`/`y` su un figlio) rende la biforcazione pila/libero determinata esplicitamente dalla modalità propria del GENITORE, non dalla presenza casuale di un prop su un figlio — scelta esplicitamente verificata per evitare che una `width` esplicita su un figlio possa silenziosamente alterare il comportamento a pila già testato.

**Natura della decisione**: campo verificato additivo prima dell'implementazione (nessun test esistente confrontava la forma esatta di un `Box` con `toStrictEqual`/`toEqual` contro un letterale scritto a mano senza questo campo — l'unico punto a rischio, `test/layout/invariants.ts`, costruisce `Box` a mano solo per `validateBox()`, mai confrontato con l'output di `computeLayout()`). Il comportamento a pila resta byte-per-byte invariato quando `layoutMode` è assente, verificato con un test di regressione a `toEqual` sull'intero Box Tree (`test/layout/libero.test.ts`).

**Evidenza disponibile**: 10 nuovi test in `test/layout/libero.test.ts` — posizionamento esplicito, ancora del contenitore che trascina i figli liberi, bounds-check condizionale, oltre ai casi in D-015.

**Rivalutazione**: quando le guide di allineamento avanzate (PRODUCT_DESIGN.md, sez. 7) richiederanno di leggere `mode` per decidere quali box offrono snap; quando/se emergerà una terza modalità di disposizione (es. griglia — PRODUCT_DESIGN.md, sez. D, "griglia che va a capo", esplicitamente non ancora analizzata).

---

## D-015 — Coordinate locali e dimensionamento esplicito-o-automatico in modalità libera (Fase 5, Blocco B)

**Stato**: un figlio posizionato liberamente ha `resolvedProps.x`/`y` interpretati come offset LOCALE rispetto all'ancora assoluta del proprio contenitore (default 0 se assente), sommato all'ancora per ottenere la posizione assoluta nel Box Tree. La larghezza è obbligatoria senza default per un nodo senza figli propri posizionato liberamente (nessun fallback), estesa anche a un nodo CON figli la cui modalità propria è "pila" (una pila non ha un concetto di larghezza calcolata dal contenuto, eredita sempre dall'alto — quindi, priva di un'ereditarietà disponibile perché il genitore è libero, non ha alcun modo di determinare la propria larghezza se non esplicito). Un contenitore la cui modalità propria è "libero", senza `width`/`height` esplicite, usa un riquadro automatico che racchiude i figli: l'ancora del contenitore resta il punto di riferimento e il riquadro si allarga solo verso l'esterno per includere ogni figlio (anche con offset negativo), senza mai ri-basare le coordinate dei figli.

**Motivazione**: coordinate locali (anziché assolute) scelte esplicitamente perché rendono "gratuito" lo spostamento di un contenitore che trascina con sé i figli liberi, e preservano l'economia Desktop-first (non serve ri-overridare ogni figlio quando si sposta un contenitore su una fascia più stretta). Il riquadro automatico ancorato (anziché un bounding-box puro dei soli figli) è stato necessario per restare compatibile con un genitore a pila: un bounding-box che ignora l'ancora del genitore fa "vagare" il contenitore rispetto alla posizione che la pila gli ha assegnato, rompendo il contenimento anche senza alcuno sconfinamento negativo — verificato empiricamente durante l'implementazione (primo giro di test fallito, corretto prima del commit).

**Natura della decisione**: due default impliciti non decisi esplicitamente altrove, applicati per coerenza interna: offset locale assente → 0; altezza di un figlio libero senza figli propri, se assente, → `DEFAULT_LEAF_HEIGHT` (40, la stessa costante già usata in modalità pila), non un valore obbligatorio come la larghezza (la Decisione 3 del proprietario del prodotto restringeva l'obbligo alla sola larghezza).

**Evidenza disponibile**: `test/layout/libero.test.ts` — riquadro automatico con e senza sconfinamento negativo, larghezza obbligatoria (leaf e contenitore-a-pila-senza-larghezza-ereditabile), conseguenza collaterale testata esplicitamente: un contenitore libero che si espande in negativo può comunque violare `CHILD_OUT_OF_BOUNDS` se il SUO genitore è a pila (comportamento corretto e voluto, non un bug).

**Rivalutazione**: quando le guide di allineamento (non ancora implementate, vedi analisi separata di questo turno) definiranno come vengono proposti gli snap sui bordi di un riquadro automatico che si sposta a ogni modifica dei figli.

---

## D-016 — Selezione separata da undo/redo dentro `History` (Fase 5, Blocco C)

**Stato**: `History` possiede `#selection: NodeId | null` (singola, non un insieme), con `select()`/`deselect()`/`get selection()`. Campo indipendente da `#past`/`#present`/`#future`: `select()`/`deselect()` non chiamano mai `applyCommand`, non creano una voce di undo/redo, non vengono toccati da `execute()`/`undo()`/`redo()`.

**Motivazione**: il codice di `History` dichiara esplicitamente se stesso come "il livello di stato 'Workspace'" a cui fa riferimento RFC-000 §1 (No Hidden State) — la selezione è stato di sessione dell'editor, non deve vivere in `useState` locale della UI. Verificato prima dell'implementazione che l'estensione fosse strutturalmente sicura: `undo()`/`redo()` in Fase 1-2 toccano solo i tre array esistenti, senza possibilità di toccare un campo indipendente aggiunto in seguito, per costruzione.

**Natura della decisione**: conseguenza esplicita e voluta del disaccoppiamento, non decisa altrove: la selezione NON viene convalidata contro il Document corrente. Se il nodo selezionato scompare (`DELETE_NODE`, o `undo()` di una `CREATE_NODE`), la selezione resta "pendente" (punta a un `nodeId` inesistente) finché qualcosa non chiama di nuovo `select()`/`deselect()`. Un consumer (renderer-react) deve trattare `selection` come potenzialmente non risolvibile — gestito esplicitamente in `PropertyPanel.tsx`.

**Evidenza disponibile**: `test/selection.test.ts` (8 test) — inclusa la verifica esplicita che `undo()`/`redo()` non toccano la selezione anche quando il nodo selezionato scompare.

**Rivalutazione**: se in una fase futura si deciderà selezione multipla (esplicitamente fuori scope per Fase 5, PRODUCT_DESIGN.md Decisione 5) — la forma `NodeId | null` andrebbe sostituita, non estesa.

---

## D-017 — `History.activeBreakpoint` (Fase 5, Blocco D)

**Stato**: `History` possiede `#activeBreakpoint: BreakpointName`, default `"desktop"`, con `get activeBreakpoint()`/`setActiveBreakpoint()`. Stesse garanzie di `#selection` (D-016): separato da undo/redo, nessun comando. A differenza della selezione, `setActiveBreakpoint()` convalida il nome contro l'elenco dei breakpoint noti (`resolver/breakpoints.ts`, interno — D-010), lanciando su un nome sconosciuto.

**Motivazione**: decide se una scrittura di un consumer va sui props base di un nodo (vista "desktop", la fascia di default per la convenzione Desktop-first di PRODUCT_DESIGN.md, Decisione 1) o dentro `props.responsive.<fascia>` (viste più strette) — è stato di sessione dell'editor con lo stesso status della selezione, non un valore locale della UI, per lo stesso principio RFC-000 §1 di D-016. La convalida (a differenza della selezione) è stata scelta perché un nome di fascia sconosciuto non ha un "nodo scomparso" plausibile da tollerare: è quasi certamente un refuso di chi chiama.

**Evidenza disponibile**: `test/activeBreakpoint.test.ts` (6 test).

**Rivalutazione**: quando le fasce responsive verranno estese oltre le 3 attuali (PRODUCT_DESIGN.md, sez. 8) — `setActiveBreakpoint` non richiede modifiche (già generico), ma la costante di sola UI `renderer-react/src/breakpoints.ts` (che duplica l'ORDINE dei nomi, non i `minWidth`, perché quell'ordine non è pubblico — D-010) andrebbe aggiornata in corrispondenza.

---

## D-018 — Adattatore di scrittura Desktop-first: separazione geometria/contenuto e congelamento automatico (Fase 5, Blocco D)

**Stato**: `renderer-react` (non l'Engine) implementa `buildUpdatePropsCommand`, che instrada ogni scrittura in base a due elenchi CHIUSI: GEOMETRIA (`x`, `y`, `width`, `height`, `layoutMode`) e CONTENUTO (`text`, `color`); qualunque altra chiave lancia un errore esplicito. Il CONTENUTO scrive sempre sui props base, indipendentemente dalla vista attiva. La GEOMETRIA, se la vista attiva è la fascia base, scrive direttamente sui props base; altrimenti scrive sulla fascia attiva e "congela" automaticamente (Opzione A) la prima fascia più larga priva di override proprio per quella chiave, al suo valore RISOLTO per quella fascia (via `resolveNode`, non il valore di base) — un solo comando `UPDATE_PROPS` per gesto, anche quando tocca più fasce.

**Motivazione**: il Resolver (Fase 2, invariato) applica una cascata tecnicamente mobile-first (un override lasciato solo su una fascia stretta si propaga verso le fasce più larghe se queste non hanno un proprio override) — comportamento verificato e documentato in PRODUCT_DESIGN.md, sez. 6, Decisione 1, che richiede esplicitamente che l'editor imponga la convenzione Desktop-first invece di lasciarla ambigua. Senza congelamento, un cambiamento fatto solo su una fascia stretta (es. Mobile) si propagherebbe silenziosamente anche a Tablet/Desktop — verificato concretamente prima di scegliere l'Opzione A (vedi turno di analisi dedicato). Il congelamento usa il valore RISOLTO per fascia (non il valore di base) su richiesta esplicita del proprietario del prodotto, per non perdere un override già presente su una fascia intermedia. La separazione geometria/contenuto (due elenchi chiusi, non un'euristica) e "il contenuto non varia mai per fascia sullo stesso nodo" sono entrambe decisioni esplicite del proprietario del prodotto, non dedotte dal codice.

**Natura della decisione**: vive interamente in `renderer-react` (`src/write/buildUpdatePropsCommand.ts`), non nell'Engine — coerente con PRODUCT_DESIGN.md riga 79 ("vincolo di design della UI derivato da un comportamento del motore, non un cambiamento del motore stesso"). L'algoritmo esamina solo la prima fascia più larga priva di override proprio per una chiave (non tutte le fasce più larghe): una volta che una chiave ha un override esplicito su una fascia T (preesistente o appena congelato), quell'override vince per costruzione su ogni fascia ancora più larga nella cascata del Resolver, quindi non serve congelare oltre — dimostrato in `DECISIONS.md` con un ragionamento per induzione, verificato dai test (congelamento su Mobile non tocca Desktop quando Tablet ha già un override proprio).

**Conseguenza nota, non risolta**: l'indicatore "ereditato vs cambiato" (PRODUCT_DESIGN.md, Decisione 5) può distinguere solo 2 stati su 3 possibili (ereditato / con override su questa fascia) — un override scritto a mano e uno scritto dal congelamento automatico hanno esattamente la stessa forma nel Document, quindi non sono distinguibili senza un metadato di provenienza non ancora progettato (vedi PRODUCT_DESIGN.md, sez. 6, aggiornamento Blocco D, Opzione 1 preferita per il futuro).

**Evidenza disponibile**: `test/write/buildUpdatePropsCommand.test.ts` (11 test, inclusi i casi di congelamento per-chiave indipendente e "Tablet ha già un override proprio"). Verificato anche in un browser reale (Vite + Chromium via CDP): trascinamento su Mobile → congelamento corretto su Tablet al valore preesistente → Desktop invariato → un solo `undo()` annulla l'intero gesto (compreso il congelamento).

**Rivalutazione**: se si deciderà di implementare il metadato di provenienza (sopra) — toccherebbe l'Engine (il Resolver dovrebbe "spacchettare" il metadato prima di leggere il valore), quindi meriterebbe una propria voce di decisione quando ripreso.

---

## D-019 — Resolver multi-asse: 7 fasce nominate, ordine di cascata curato a mano (Fase 6)

**Stato**: le 3 fasce lineari di Fase 2 (`mobile`/`tablet`/`desktop`, solo `minWidth`) sono sostituite da 7 fasce nominate — `mobile-verticale`, `mobile-orizzontale`, `tablet-verticale`, `tablet-orizzontale`, `laptop-compatto`, `desktop-compatto`, `desktop` — ciascuna con un predicato descrittivo proprio (`minWidth?`/`maxWidth?`/`orientation?`/`minHeight?`/`maxHeight?`, tutti opzionali e indipendenti). Soglie prese *verbatim* dall'audit del sito reale Vicolo Cechov (Fase 6A, righe 62-69 del sorgente HTML), non stimate. `BASE_BREAKPOINT` (= `"desktop"`) sostituisce il precedente concetto implicito di fascia base. Sostituzione pulita, non convivenza: `CURRENT_SCHEMA_VERSION` passa da 1 a 2 (`document/types.ts`).

**Motivazione — perché "combinazioni nominate" e non assi ortogonali indipendenti o predicati stile media-query**: verificato contro i 7 dati reali (non assunto): 3 delle 7 fasce non hanno alcun vincolo di orientamento, e le fasce si sovrappongono (es. "Laptop compatto" 1025-1199 senza vincolo e "Tablet orizzontale" 768-1199 landscape+altezza≥551 coprono entrambe un landscape largo 1100px alto 600px) — il CSS reale risolve questo con l'ordine di dichiarazione nel foglio di stile, non con una partizione pulita. Un modello ad assi ortogonali pienamente indipendenti avrebbe costruito una capacità (ogni combinazione width×orientation indirizzabile) che i dati reali non usano. Un modello a predicati stile media-query (matching contro un viewport reale) risolverebbe un problema che oggi non ha alcun consumer: `ResolverContext.breakpoint` è sempre un nome già scelto (dall'autore, tramite un pulsante), mai una larghezza grezza da far combaciare — nessun punto del codice fa mai matching pixel-reali→fascia; quel bisogno esisterà solo con un runtime pubblicato (l'Exporter), esplicitamente fuori scope. Le "combinazioni nominate" sono la generalizzazione minima del sistema già approvato: le 3 fasce di Fase 2 erano già, letteralmente, un caso degenere di questo stesso schema.

**Ordine di cascata (Punto 2 dell'analisi delle fondamenta)**: non derivato da una formula su larghezza/orientamento/altezza — verificato con un caso concreto che una formula ingenua sbaglierebbe (un override lasciato su una fascia stretta CON vincolo di orientamento, es. `mobile-verticale`, non deve propagarsi a una fascia larga SENZA vincolo, es. `desktop`, anche se "più larga" in termini di soli pixel: un override pensato per un telefono in verticale non ha senso su un monitor desktop). Regola curata a mano, verificata voce per voce: le due catene nella stessa diramazione di orientamento cascatano (`mobile-verticale`→`tablet-verticale`; `mobile-orizzontale`→`tablet-orizzontale`), le tre fasce senza vincolo di orientamento (`laptop-compatto`, `desktop-compatto`, `desktop`) sono bende indipendenti, nessuna eredita dalle altre né da fasce con vincolo di orientamento.

**Superficie pubblica (reversione mirata di D-010)**: `BREAKPOINTS` (l'array intero coi predicati) resta interno, invariato nella sostanza di D-010. Aggiunte 3 funzioni pubbliche minime — `listBreakpointNames()`, `widerBreakpoints(name)`, `BASE_BREAKPOINT` — perché `renderer-react` (consumer esterno reale) ne ha bisogno per il pulsante di cambio vista e per il congelamento Desktop-first, esattamente il trigger di rivalutazione che D-010 aveva già previsto esplicitamente ("quando emergerà un consumer esterno reale... che necessiti di conoscere i nomi di breakpoint validi dall'esterno del package"). `renderer-react/src/breakpoints.ts` non duplica più localmente l'ordine dei nomi (rischio già segnalato in D-017/Blocco D, ora risolto invece che solo documentato).

**Difetto trovato e corretto durante la re-derivazione**: l'algoritmo di congelamento di D-018 (`buildFrozenResponsive`) esaminava solo il *primo* elemento di `widerTiers()`, assumendo (vero per una catena lineare a 3 fasce, mai verificato esplicitamente per il caso generale) che bastasse. Con `widerBreakpoints` potenzialmente in grado di restituire più vicini diretti indipendenti (non ancora il caso con le 7 fasce attuali, ma non escluso in futuro), quell'assunzione sarebbe stata sbagliata: congelare solo il primo vicino e ignorare gli altri lascerebbe una fascia indipendente non protetta. Riscritto per processare ogni vicino diretto indipendentemente (l'intero insieme di chiavi cambiate, non un pool che si esaurisce) — dimostrato corretto anche per un grafo non lineare: una fascia ancora più larga che include un vicino diretto (ora congelato) nella propria cascata riceve comunque il valore corretto durante la propria risoluzione, senza bisogno di risalire la catena qui.

**Evidenza disponibile**: `test/resolver/breakpoints.test.ts` (19 test: predicati di tutte le 7 fasce, ordine di cascata verificato voce per voce incluso il caso "non si propaga tra diramazioni diverse", `widerBreakpoints`). `test/write/buildUpdatePropsCommand.test.ts` (14 test, riscritti sulle 7 fasce, incluso il caso "editare una fascia senza vicini più larghi non congela nulla" - comportamento nuovo rispetto a Fase 5, dove ogni fascia aveva sempre un "successivo"). Verificato anche in un browser reale: trascinamento su `mobile-verticale` → congelamento corretto su `tablet-verticale` al valore preesistente → `laptop-compatto`/`desktop` invariati (non solo "schermati dal congelamento", isolati per costruzione); trascinamento su `laptop-compatto` (nessun vicino più largo) → nessun congelamento, `desktop` invariato. Suite complete del monorepo verdi (Engine 163, CLI 20, Test Runner 3, renderer-react 59).

**Rivalutazione**: se emergerà un vero consumer che deve far combaciare un viewport reale contro le fasce (l'Exporter) — a quel punto servirà un valutatore di predicati, esplicitamente non costruito qui. Se le fasce reali del sito cambieranno (nuove soglie, nuovi dispositivi), `BREAKPOINTS`/`CASCADE_ORDER` vanno aggiornati insieme, a mano, con la stessa verifica voce-per-voce fatta qui - non c'è una formula da cui i due si derivano automaticamente.

---

## D-020 — Motore di navigazione pagine/scene: convenzione `type==="scene"`, transizioni CSS, nessuna estensione del Document Model o di History (Fase 7)

**Stato**: nuovo pacchetto di funzionalità interamente in `renderer-react`, nessuna modifica all'Engine. Una "scena" (Punto 1 dell'analisi, Opzione B) è un figlio diretto della radice di una pagina il cui `DocumentNode.type` vale `"scene"` — nessuno schema dedicato, `type` resta la stringa libera che è da Fase 1 (`preview/scenes.ts`, `sceneNodeIds()`). `ElementType` (renderer-react) si estende con `"scene"` (`elements/createElementCommand.ts`), sempre creata come figlia diretta della radice pagina indipendentemente dalla selezione corrente (`ElementPalette.tsx`), a differenza di "testo"/"contenitore" che rispettano un contenitore libero selezionato. Un nuovo componente `Preview.tsx` (non un riuso di `Canvas.tsx`) renderizza staticamente l'albero di una pagina (riusa `flattenBoxes`, non la logica di drag/resize/selezione) e naviga pagine/scene con le frecce tastiera, transizioni `transition` CSS native (nessun motore rAF/easing custom), un unico lock (`isTransitioning`) che ignora input durante una transizione in corso. Stato di navigazione (`PreviewPosition {pageId, sceneIndex}`) locale al componente `Preview`, non aggiunto a `History`.

**Motivazione — perché una convenzione e non uno schema dedicato**: coerente con la raccomandazione già scritta in PRODUCT_DESIGN.md sez. 2 ("Scena: convenzione, nessuno schema dedicato"), ed evita di forzare ogni pagina esistente/futura a essere "a scene" per costruzione (l'alternativa strutturale — ogni figlio diretto della radice è una scena — avrebbe reso navigabile per forza anche un header fisso o altro contenuto non pensato per lo scorrimento). Nessuna modifica allo schema (`DocumentNode.type` è già libero), quindi nessun bump di `CURRENT_SCHEMA_VERSION`.

**Motivazione — perché stato locale e non `History`**: stesso principio di D-016/D-017 applicato nella direzione opposta — qui la posizione di navigazione NON è stato di sessione dell'editor allo stesso titolo di selezione/fascia attiva, è "dove sto guardando", non "cosa ho modificato": un `undo()` durante la navigazione in Preview non deve annullarla né esserne influenzato. Per analogia diretta e verificata col precedente già stabilito per `activePageId` in Fase 5 Blocco E (stato locale di `App.tsx`, non `History`, per lo stesso tipo di motivazione).

**Motivazione — perché `transition` CSS e non il motore rAF del sito reale**: il motore rAF/easing custom del sito reale (Fase 6A) esiste lì solo per sincronizzare `aggiornaColoreHeader()` (inversione chiaro/scuro dell'header) frame-per-frame con la posizione di scroll — funzionalità esplicitamente fuori scope qui, rimandata a B/S7/Fase 13 (stato/variabili, non ancora decisa architetturalmente). Senza quel requisito, una `transition` dichiarativa è sufficiente e enormemente più semplice. Per il cambio scena (stesso albero di pagina) basta una sola `transition: transform` sull'unico elemento montato. Per il cambio pagina (albero diverso, radice diversa) una dissolvenza incrociata a due fasi (opacità a 0 → sostituzione contenuto a opacità 0 → opacità a 1, sequenziata con un doppio `requestAnimationFrame` per garantire che il nuovo contenuto sia dipinto a opacità 0 prima di animare verso 1) evita di dover montare contemporaneamente due alberi di pagina per un semplice scorrimento orizzontale — dettaglio implementativo lasciato aperto dall'analisi (Punto 4 non specificava il meccanismo per asse), deciso qui.

**Difetto trovato e corretto durante la verifica in browser**: i default approvati per il nuovo tipo "scene" inizialmente includevano solo `height` (assumendo che la radice pagina fosse sempre in modalità "pila", dove `x`/`y`/`width` di un figlio sono irrilevanti — ereditati dall'alto). Verificato in browser sul documento demo esistente (la cui radice è esplicitamente "libero", per il demo di drag libero delle card di Blocco B/D): `computeLayout` lanciava un errore ("width" obbligatoria assente) creando una scena lì, perché un figlio senza propri figli posizionato in modalità libera dal genitore richiede `width` esplicita (Decisione 3, D-014/D-015) — caso non coperto dai test unitari esistenti, che non creano mai un elemento sotto una radice "libero". Corretto aggiungendo `x:0, y:0, width:800` ai default (ignorati quando il genitore è "pila", dove contano comunque solo per un fallback comunque innocuo). Un secondo effetto collaterale osservato nello stesso test in browser (non un bug, un limite noto): sotto una radice "libero", scene create in sequenza si sovrappongono tutte a `(0,0)` invece di impilarsi (l'impilamento automatico è una proprietà della sola modalità "pila") — per questo l'uso previsto delle scene presuppone una pagina con radice "pila" (il default per una pagina nuova), non la pagina demo esistente, il cui scopo resta quello originale (drag libero) e non è pensata per la navigazione a scene.

**Evidenza disponibile**: `test/preview/scenes.test.ts` (4 test — filtro per convenzione, ordine, esclusione di un nipote non diretto), `test/preview/navigation.test.ts` (8 test — avanzamento, clamp ai confini senza wrap su entrambi gli assi, reset di `sceneIndex` al cambio pagina, pagina/nodo sconosciuti), `test/elements/createElementCommand.test.ts` (+4 test: default della scena, regressione del difetto sopra sotto entrambe le modalità di radice). Verificato in un browser reale (Vite + Chromium via CDP): 3 scene create su una pagina nuova (radice "pila") → apertura Preview → `ArrowDown` sposta `translateY` di -400px (l'altezza di una scena) per volta, clampato a -800px oltre la terza scena (nessun wrap) → `ArrowUp` torna a -400px → `ArrowLeft` verso la pagina "Home" (senza scene) mostra correttamente il fallback statico dell'intera pagina (Punto 1) → `ArrowRight` torna a "Landing" → `Escape` chiude la Preview e ripristina il Canvas di editing, zero errori console durante l'intero percorso. Suite complete del monorepo verdi (Engine 163, CLI 20, Test Runner 3, renderer-react 74).

**Rivalutazione**: quando verrà affrontata l'architettura di stato/variabili (B/S7/Fase 13) — la posizione di navigazione costruita qui (`PreviewPosition`) è il punto di aggancio naturale per una futura funzionalità che debba reagire alla scena/pagina corrente (es. l'inversione dell'header, rimandata); quando (e se) verrà introdotto routing/deep-link (Punto 7, rimandato) — riguarderà un output pubblicato, non l'editor, da rivalutare con l'Exporter.
---

## D-021 — `MOVE_NODE`: riassegnazione del genitore di un nodo esistente, primitiva generale dell'Engine (Fase 8, precondizione)

**Stato**: nuovo comando `MOVE_NODE {nodeId, newParentId, index?, props?}` (`packages/engine/src/runtime/commands.ts`). Aggiorna atomicamente `childrenIds` del vecchio genitore, `childrenIds` del nuovo genitore e `parentId` del nodo stesso in un'unica `Map` scritta prima di restituire il nuovo `Document` — stesso pattern già usato da `applyCreateNode`/`applyDeleteNode` (più voci scritte insieme, nessuno stato intermedio osservabile). `index` assente → fine lista; se `newParentId` coincide col genitore attuale (riordino), `index` è interpretato DOPO aver tolto il nodo dalla lista (decisione esplicita del proprietario del prodotto: "posizione finale desiderata tra gli altri figli", comportamento intuitivo di un drag-and-drop di riordino). `props` è un'eccezione MINIMA — vincolo esplicito del proprietario del prodotto: serve SOLO a permettere, nello stesso comando/passo di undo, di aggiornare le coordinate `x`/`y` locali che uno spostamento tra contenitori "libero" può rendere insensate (D-015: offset relativo all'ancora del contenitore ATTUALE); non è una seconda via generale di scrittura — qualunque altro aggiornamento di proprietà continua a passare da `buildUpdatePropsCommand` (renderer-react), l'unico che applica il congelamento responsive Desktop-first (D-018).

**Motivazione**: nessun comando esistente permetteva di cambiare il genitore di un nodo dopo la creazione (verificato: `applyUpdateProps` tocca solo `props`, `CREATE_NODE` fissa `parentId` una volta sola) — capacità mancante, non specifica della griglia (Fase 8): il comando non legge mai `layoutMode` del nuovo genitore, quindi vale identicamente per "pila"/"libero"/"griglia". Guardie esplicite (stile "fallisce in modo leggibile", stesso trattamento di `applyDeleteNode` sulla radice di pagina): rifiuta di spostare un nodo con `parentId === null` (radice di pagina), di rendere un nodo genitore di se stesso, e di spostarlo dentro un proprio discendente (ciclo — controllo esplicito via `collectSubtreeIds`, riusata invariata, PRIMA di costruire il documento, per un messaggio di errore leggibile; la rete di sicurezza generica di `assertValidDocument`, MULTIPLE_PARENTS/ORPHAN_PARENT_LINK/CYCLE_DETECTED, resta comunque l'ultima linea di difesa strutturale, verificata già esistente e non modificata).

**Renderer minimo (Punto 6 dell'analisi, Opzione B)**: azione esplicita a due click ("Sposta dentro…"), non drag-and-drop tra contenitori (rimandato, additivo sopra lo stesso comando quando servirà) — stesso principio già seguito in Fase 7 (interazione più semplice e deterministica prima, gestualità più fluida dopo). Stato locale del `Canvas` (`moveSourceId`/`moveError`), stesso livello di `moveDrag`/`resizeDrag` esistenti — non stato di sessione. Unico punto del Canvas che intercetta un errore di `store.execute` con un `try/catch` esplicito (il resto del Canvas non lo fa, coerente con l'assenza di gestione errori altrove): qui il bersaglio è scelto dall'utente via click, e un tentativo non valido (es. un ciclo) è un esito plausibile, non un bug.

**Evidenza disponibile**: `test/commandBus.test.ts` (13 nuovi test — spostamento tra genitori diversi, indice esplicito, riordino nello stesso genitore con la semantica "dopo la rimozione" verificata con due casi che la distinguono esplicitamente dall'alternativa non scelta, purezza, merge di `props`, tutte le guardie, funziona tra sottoalberi di pagine diverse). `test/history.test.ts` (+1: un solo comando con `props` → un solo `undo` ripristina sia il genitore sia le coordinate). Verificato in un browser reale: spostamento riuscito con la matematica di posizione attesa (l'ancora del nuovo genitore + l'offset locale invariato del figlio), tentativo di ciclo respinto con errore visibile e nessuna modifica al documento, un solo `undo` che ripristina l'intero gesto.

**Rivalutazione**: quando (se) verrà costruito il drag-and-drop tra contenitori (Punto 6, Opzione A) — userà lo stesso comando, serve solo l'hit-testing nel Canvas. Quando verrà decisa l'architettura di stato/variabili (S7/Fase 13) — nessun impatto atteso, `MOVE_NODE` non tocca variabili globali.

---

## D-022 — Layout Grid, nucleo: modalità "griglia" (N colonne uguali + gap, nessuna cella nel Document Model) (Fase 8)

**Stato**: terza modalità di `layoutMode` (dopo "pila"/"libero", D-014/D-015) — `"griglia"`. `Box.mode` esteso a `"pila" | "libero" | "griglia"`. Un nodo in modalità "griglia" dispone i figli che GIÀ ha in `childrenIds` (nessun figlio creato o limitato dalla griglia stessa — correzione esplicita del proprietario del prodotto ricevuta prima dell'implementazione: NON esiste alcun concetto di "cella" nel Document Model) in righe da `columns` elementi, larghezza di cella `(larghezza - gap×(columns-1))/columns`, altezza di riga = altezza della cella più alta DI QUELLA riga (non uniforme sull'intera griglia). `columns` obbligatorio senza default (un default silenzioso, es. 1, trasformerebbe silenziosamente una griglia a card in una pila verticale); `gap` opzionale, default 0. Nessuna cella vuota per un numero di figli non multiplo di `columns` (l'ultima riga resta parziale). Generalizzazione diretta dell'algoritmo "pila" esistente (stesso riuso di `layoutNode` per ogni figlio, un ciclo a due indici invece di uno) — non un motore di layout nuovo.

**Motivazione — perimetro "nucleo"**: rideterminato dall'audit del sito reale (`sito_vicolo.html`, stessa fonte di 6A): 3 usi su 4 di `display:grid` sono esattamente "N colonne uguali + gap, N esplicito per fascia" (`.porte-cards`, `.card-grid.g3/.g4`); 1 uso è `auto-fit` intrinseco (`.voci-grid`), esplicitamente fuori scope (nessun override per fascia nel CSS, sostituibile in futuro con un modo alternativo di derivare `columns` sopra lo stesso algoritmo, senza rifare nulla). Nessuno spanning multi-cella, nessuna area nominata, nessun dimensionamento di riga esplicito in nessun uso reale (verificato via grep sull'intero sorgente: zero occorrenze) — esclusi esplicitamente dal nucleo. `.header-inner` (3 colonne asimmetriche `1fr auto 1fr`) escluso: componente singolo, non generalizzabile, passa comunque a flex sotto breakpoint.

**Nessuna modifica a `layout/invariants.ts`**: `CHILD_OUT_OF_BOUNDS` si applica già a "qualunque modalità diversa da libero" (D-014), quindi copre "griglia" senza toccare il codice — il contenimento vale per costruzione (ogni cella riceve una posizione/larghezza calcolata per stare esattamente dentro la riga/colonna assegnata), dimostrato con un test dedicato, non assunto per analogia. Un `gap` eccessivo rispetto alla larghezza disponibile produce una larghezza di cella negativa: NESSUN clamp silenzioso — lasciato fluire fino a `NEGATIVE_DIMENSION` (stessa filosofia "fallisce in modo visibile" di `requireExplicitWidth`), verificato con un test dedicato.

**Renderer minimo (perimetro "nucleo" secondo l'analisi, Punto 7 — Opzione B)**: un pulsante "+ Griglia" in `ElementPalette.tsx` (stesso pattern di "+ Scena", Fase 7) che crea un contenitore vuoto (`nodeType:"box"`, `layoutMode:"griglia"`, `columns:3`/`gap:16` di default — `[Proposta]` non vincolante). A differenza di "scene" (parent fisso sulla radice pagina), una griglia segue la regola STANDARD di collocamento (`resolveNewElementParent`, invariata) — può stare ovunque un contenitore normale può stare. Nessun controllo UI per `columns`/`gap` per fascia (S1, rimandato) e nessuna editabilità post-creazione nel `PropertyPanel` (ambito minimo, D7, invariato) — un utente che vuole un numero di colonne diverso dal default deve ricreare la griglia in questa fase, limite noto e segnalato. Popolare una griglia con elementi esistenti usa `MOVE_NODE` (D-021), costruito come precondizione esplicita di questa fase.

**Evidenza disponibile**: `test/layout/griglia.test.ts` (12 test — disposizione base, gap su entrambi gli assi, riga parziale senza celle fantasma, altezza per riga non uniforme, `columns` assente/non valido con e senza figli, nessuna violazione di contenimento, larghezza di cella negativa non clampata, ereditarietà della larghezza identica a "pila" sia da un genitore "pila" sia da un genitore "libero"). `test/elements/createElementCommand.test.ts` (+3: default della griglia, nessun crash sotto radice "libero"/"pila" da vuota). Verificato in un browser reale: creazione di una griglia vuota, inserimento di un elemento esistente via "Sposta dentro…" (MOVE_NODE), il figlio risultante coincide esattamente con l'ancora della griglia (colonna 0, riga 0) — nessun offset locale x/y proprio del figlio, a differenza di "libero". Suite complete del monorepo verdi (Engine 191, CLI 20, Test Runner 3, renderer-react 77).

**Rivalutazione**: S1 (colonne/gap per fascia, UI dedicata nel `PropertyPanel`) resta l'estensione naturale, non ancora avviata. `auto-fit` (Punto 8 dell'analisi): quando ripreso, deriva `columns` diversamente PRIMA dello stesso algoritmo a righe — non richiede di rifare il nucleo. Drag-and-drop per popolare una griglia (invece dell'azione esplicita "Sposta dentro…"): stessa nota di rivalutazione di D-021.

---

## D-023 — Tipografia fluida, nucleo: `fontSize` come stringa `clamp()` letterale, opaca per l'Engine (Fase 10)

**Stato**: `ElementType` `"text"` (renderer-react) porta ora un default `fontSize: "clamp(16px, 2vw, 24px)"` nei propri `props`. Nessuna modifica al Document Model, al Resolver, al Layout Engine: `fontSize` è un prop libero come `text`/`color` (CONTENT_KEYS, D-018), mai interpretato dall'Engine, mai letto da `computeLayout`. Il valore è una stringa CSS `clamp()` LETTERALE (non una tripla `{min,preferred,max}` da assemblare, non un token di scala) — il Renderer (`Canvas.tsx`/`Preview.tsx`) la assegna direttamente a `style.fontSize`, con fallback al valore fisso preesistente (12) se il prop è assente (documenti/nodi creati prima di questa fase).

**Motivazione — ordine rispetto a Fase 9**: verificato (non assunto) prima di scegliere l'ordine — `sito_vicolo.html`: il 100% dei `font-size` reali (heading, paragrafi, link/bottoni) è un `clamp()`, zero casi con una dimensione fissa "a riposo". Se Fase 9 (tipi di elementi: heading/paragrafo/link) fosse stata costruita prima con default fissi in px, Fase 10 li avrebbe dovuti rifare tutti — rischio di rilavoro certo, non ipotetico, stesso principio già usato per ordinare M1 prima di S1/S2 (6C). Nessuna dipendenza nella direzione opposta: questa fase è dimostrata sul tipo `"text"` già esistente, senza bisogno che heading/paragrafo/link esistano.

**Motivazione — perché una stringa letterale e non una tripla strutturata o un token di scala**: verificato contro i dati reali - decine di `clamp()` distinti nel sito, uno bespoke per elemento (`hero-title` 48-300px, `.porta-tit` 20-30px, ecc.), nessuna evidenza di una scala fissa riusata (scarta l'ipotesi "token"). Una tripla strutturata avrebbe richiesto una funzione di assemblaggio da subito per un bisogno (editing a campi separati nel `PropertyPanel`) che questo nucleo non prevede ancora (mirror esatto della scelta "nessuna cella" per la griglia, D-022, e "nessuna UI di editing" per `columns`/`gap`: nucleo minimo, capacità di editing strutturato rimandata). Stesso trattamento di `color` oggi: nessuna validazione di formato — un valore malformato produce un `font-size` CSS invalido, ignorato in modo innocuo dal browser.

**Questione lasciata esplicitamente aperta (non decisa qui, per istruzione esplicita)**: se `fontSize` debba entrare nell'elenco chiuso `GEOMETRY_KEYS` di `buildUpdatePropsCommand.ts` (D-018) — riusando il comportamento di congelamento Desktop-first, perché il sito reale mostra formule `clamp()` diverse per fascia, non solo scaling continuo entro una fascia (es. `--s1-slide2-titolo-fs-base` sovrascritta dentro una media query) — o se meriti una terza categoria semanticamente propria. Irrilevante per il nucleo (nessuna scrittura di `fontSize` passa oggi da `buildUpdatePropsCommand`, il default si scrive una sola volta a `CREATE_NODE`), ma da sottoporre esplicitamente PRIMA che S2 (tipografia per fascia) inizi.

**Evidenza disponibile**: `test/elements/createElementCommand.test.ts` (aggiornato: il default di `"text"` include `fontSize`). Verificato in un browser reale: un nodo "text" con `fontSize: clamp(16px, 2vw, 24px)` misurato via `getComputedStyle` a tre larghezze di finestra — 24px (il massimo) a 1400px, 16px (il minimo) a 500px, 20px esatto (il valore preferito, 2vw) a 1000px — mentre un nodo preesistente senza `fontSize` esplicito resta fisso a 12px in tutti e tre i casi, invariato dal ridimensionamento. Zero errori console. Suite complete del monorepo verdi (Engine 191, CLI 20, Test Runner 3, renderer-react 77).

**Rivalutazione**: prima di S2 (tipografia per fascia) — va sottoposta la classificazione GEOMETRY_KEYS/terza categoria (sopra). Quando Fase 9 introdurrà heading/paragrafo/link, ciascun default assegnerà un proprio `fontSize` usando lo stesso meccanismo qui costruito (nessuna modifica a questo nucleo prevista).

---

## D-024 — Tipi di elementi minimi: `h1`/`h2`/`h3`/`paragraph`/`link`, un `type` per valore (Fase 9)

**Stato**: 5 nuovi `ElementType` (renderer-react): `h1`, `h2`, `h3` (distinzione heading), `paragraph`, `link` (`props.href`). Ciascuno mappa a un `DocumentNode.type` omonimo (`"h1"`, `"h2"`, `"h3"`, `"paragraph"`, `"link"`) — nessuno schema dedicato nel Document Model (`type` resta la stringa libera invariata da Fase 1). Seguono la regola di collocamento STANDARD (`resolveNewElementParent`), non quella fissa di "scene". Nuovo modulo `elements/htmlTag.ts` (puro, condiviso tra Canvas.tsx e Preview.tsx): mappa `type → tag HTML` (`h1→h1`, `h2→h2`, `h3→h3`, `paragraph→p`, `link→a`, ogni altro `type` → `div`, comportamento invariato). Canvas/Preview renderizzano ora il tag reale invece di un `<div>` sempre uguale.

**Motivazione — perché `h1`/`h2`/`h3` distinti e non `type:"heading"`+`props.level`**: verificato (non assunto) contro `sito_vicolo.html` — solo 3 livelli di heading realmente usati (1× h1, 14× h2, 7× h3, zero h4-h6). Un `type` per livello è coerente con un precedente architetturale già in uso: `layoutMode` è già un piccolo insieme chiuso di stringhe (non un tipo+parametro numerico) proprio perché la sua cardinalità è piccola e nota in anticipo — la stessa proprietà vale qui, a differenza di `columns` (D-022), genuinamente numerico e illimitato, dove un tipo-per-valore sarebbe stato assurdo. Nessun consumer attuale enumera heading come gruppo (verificato: l'unico punto dell'intero codice che ispeziona `type` è `preview/scenes.ts` per `"scene"`), quindi un singolo `type:"heading"` con filtro di gruppo non avrebbe alcun beneficio concreto oggi.

**Motivazione — `link` come leaf, non contenitore**: verificato — le 28 occorrenze reali di `<a>` avvolgono sempre e solo testo semplice, mai altri elementi strutturati. Un link-contenitore resta esplicitamente fuori scope, da riconsiderare solo con un caso d'uso concreto.

**Default — nessun valore nuovo inventato**: correzione esplicita del proprietario del prodotto ricevuta prima dell'implementazione. `x`/`y`/`width`/`height`/`text`/`fontSize` per `h1`/`h2`/`h3`/`paragraph` sono ESATTAMENTE quelli già usati da `"text"` (`{x:20,y:20,width:160,height:40,text:"Testo",fontSize:"clamp(16px, 2vw, 24px)"}`) — nessuna differenziazione di dimensione per livello di heading (sarebbe una decisione di scala tipografica non ancora presa, la stessa lasciata esplicitamente aperta in D-023 per S2). `link` riusa lo stesso identico set più `href`. L'unico valore genuinamente senza precedente — `href` di un link appena creato — è stato segnalato esplicitamente (nessuna decisione nel codice o nella roadmap lo determinava) e deciso dal proprietario del prodotto prima di scrivere il codice: stringa vuota `""`.

**Tag HTML reale anche nell'editor (Canvas/Preview), non solo `div`**: compatibile con D1 (Canvas piatto, Fase 5 Blocco D) — cambia solo il tag di un box già foglia e assolutamente posizionato, non introduce alcun nesting DOM, quindi non viola quel vincolo (che riguardava la gerarchia, non la scelta del tag). Necessario perché senza un tag reale la distinzione introdotta da questa fase non sarebbe osservabile in alcun modo.

**Click su un `<a>` nell'editor**: `e.preventDefault()` nello stesso gestore `onClick` già presente su ogni box (Canvas.tsx) e nel rendering statico (Preview.tsx) — innocuo per ogni altro tag. Esteso anche al pulsante "Sposta dentro…" (D-021) quando annidato dentro un `<a>` selezionato: un click su un elemento interattivo annidato dentro un `<a>` può comunque attivare la navigazione nativa dell'anchor antenato se nessun gestore lungo il percorso chiama `preventDefault` (`stopPropagation` da solo non basta, non impedisce l'azione di default) — verificato concretamente in browser (click sul pulsante annidato, URL invariato).

**Perimetro del "nucleo"**: creazione via `ElementPalette` (5 nuovi pulsanti), nessuna UI di editing in `PropertyPanel` per `href` (resta "ambito minimo", D7 — stessa scelta di D-022/D-023). `href` non è aggiunto a `CONTENT_KEYS` in questo nucleo (nessuna scrittura passa da `buildUpdatePropsCommand` finché non c'è editing) — a differenza di `fontSize` (D-023), la sua classificazione come contenuto non è ambigua (nessuna evidenza reale di un `href` diverso per fascia responsive), quindi non genera una decisione aperta da sottoporre: resta solo un'estensione a basso rischio, rimandata per coerenza col nucleo, non per incertezza.

**Evidenza disponibile**: `test/elements/htmlTag.test.ts` (2 test — mappatura dei 5 type semantici, fallback a `div` per ogni altro type incluso quelli preesistenti). `test/elements/createElementCommand.test.ts` (+17: `elementIdBase` per i 5 nuovi tipi, default di `h1`/`h2`/`h3`/`paragraph` identici a `text` via `it.each`, default di `link`, regressione "nessun crash sotto radice libero/pila da vuota" per tutti e 5). Verificato in un browser reale: i 5 pulsanti creano elementi con `tagName` DOM corretto (H1/H2/H3/P/A); un click sul link non naviga (URL invariato) e il link resta normalmente selezionabile (il pulsante "Sposta dentro…" compare); un click sul pulsante "Sposta dentro…" annidato dentro il link selezionato non naviga nemmeno lui. Zero errori console. Suite complete del monorepo verdi (Engine 191, CLI 20, Test Runner 3, renderer-react 94).

**Rivalutazione**: se emergerà un bisogno concreto di enumerare "tutti gli heading di una pagina" come gruppo (oggi nessun consumer lo fa) — a quel punto rivalutare se un filtro su 3 stringhe (`["h1","h2","h3"].includes(type)`) resta sufficiente o serve un `type:"heading"` unificato. Se emergerà un link-contenitore (oggi nessuna evidenza reale) — va riconsiderato come una forma diversa, non un'estensione di questo. `href` in `CONTENT_KEYS`: passo a basso rischio quando servirà l'editing da UI, non una domanda aperta.

---

## D-025 — Griglia responsive per fascia (S1): terza categoria `STYLE_KEYS`, visibilità condizionale nel `PropertyPanel`

**Stato**: `columns`/`gap` (griglia, D-022) e `fontSize` (tipografia, D-023) entrano in una nuova categoria chiusa, `STYLE_KEYS` (`write/buildUpdatePropsCommand.ts`), affiancata a `GEOMETRY_KEYS`/`CONTENT_KEYS` — decisione esplicita del proprietario del prodotto, non presa in autonomia. Stesso comportamento di congelamento Desktop-first di `GEOMETRY_KEYS` (geometria e stile condividono lo stesso `buildFrozenResponsive`, chiamato una sola volta con l'unione delle due categorie — un solo `props.responsive` ricostruito per gesto, non uno per categoria), ma elenco separato: il nome `GEOMETRY_KEYS` sarebbe rimasto fuorviante per `columns`/`fontSize`, che non sono geometria del box. Questa stessa decisione chiude la domanda lasciata esplicitamente aperta in D-023 per `fontSize` — non era ancora in nessun elenco chiuso prima di questa fase, ora è in `STYLE_KEYS`.

`panel/geometryFieldState.ts` rinominato `panel/frozenFieldState.ts` (funzione `frozenFieldState`, tipo `FrozenFieldState`): stessa logica, invariata, ora tipizzata su `GeometryKey | StyleKey` invece che sulla sola `GeometryKey` — chiamarla ancora "geometry" per un campo come `columns` sarebbe stato fuorviante quanto tenerlo in `GEOMETRY_KEYS`.

**`PropertyPanel` — visibilità condizionale (decisione esplicita)**: `columns`/`gap` compaiono SOLO quando `resolvedProps.layoutMode === "griglia"` (risolto alla fascia attiva, stesso trattamento già usato per `isLiberoContainer` in `createElementCommand.ts` — un override responsive di `layoutMode` va rispettato, non solo il prop base). Primo caso di visibilità condizionale nel pannello (prima di questa fase, i 6 campi erano sempre tutti presenti, D7) — dichiarato esplicitamente pattern STANDARD per campi specifici-per-tipo, non un'eccezione una tantum: si applicherà anche a `href` (Fase 9) quando arriverà nel pannello.

**Fatto verificato prima di implementare — nessuna modifica a Engine/Resolver/Layout necessaria**: `resolveNode`'s `applyBreakpointOverrides` (`Object.assign(result, override)`) è già generico su qualunque chiave, e `computeLayout` legge già `resolvedProps.columns`/`gap` dal modello risolto per la fascia richiesta — il meccanismo di fondo funzionava già prima di questa fase; S1 è stato quasi interamente un lavoro di `renderer-react` (classificazione + UI), non dell'Engine.

**Fatto verificato contro i dati reali — i breakpoint dove cambia `columns` non mappano 1:1 sulle 7 fasce nominate**: `sito_vicolo.html` — `.porte-cards` (4→2 colonne) cambia solo dentro `@media (max-width:767px) and (orientation:portrait)`, esattamente `mobile-verticale`; `.card-grid.g3/.g4` (3o4→2 colonne) cambia dentro `@media (min-width:768px) and (max-width:1199px)`, **senza vincolo di orientamento** — un intervallo che copre contemporaneamente tre fasce nominate distinte (`tablet-verticale`, `tablet-orizzontale`, `laptop-compatto`). Nessuna fascia "raggruppo multiplo" esiste nel modello (D-019 l'ha già scartata) — replicare questo comportamento richiede impostare lo stesso valore come override esplicito su tre fasce separate, conseguenza diretta e prevista del modello a "combinazioni nominate senza raggruppamenti", non un difetto di questa fase. Segnalato, non risolto qui (nessun meccanismo "applica a più fasce in un gesto" richiesto dalla roadmap approvata).

**Evidenza disponibile**: `test/panel/frozenFieldState.test.ts` (rinominato da `geometryFieldState.test.ts`, corretti 4 letterali di fascia obsoleti — "mobile" non è più un nome di fascia valido da D-019 (Fase 6), mai aggiornati nella rinomina di allora perché `test/` non è tipizzato dalla build, `tsconfig.json` include solo `src` — bug latente trovato e corretto incidentalmente lavorando su questo file; +1 test per `columns`). `test/write/buildUpdatePropsCommand.test.ts` (+9: congelamento di `columns` mirror esatto dei test già scritti per la geometria in Fase 6, incluso "nessun vicino più largo" per `laptop-compatto`; geometria+stile nello stesso oggetto `responsive`; `gap`; `fontSize` ora accettato e congelato come qualunque `STYLE_KEYS`; messaggio d'errore aggiornato a tre categorie). Verificato in un browser reale: un nodo non-griglia non mostra `columns`/`gap`; una griglia appena creata li mostra con i default (3, 16); modificare `columns` su `mobile-verticale` produce badge "overridden-here" lì E su `tablet-verticale` (congelato al valore pre-modifica, 3) mentre `laptop-compatto`/`desktop` restano "inherited" e invariati — stessa identica verifica già fatta per `x`/`y` in D-018/D-019, ora su `columns`. Zero errori console. Suite complete del monorepo verdi (Engine 191, CLI 20, Test Runner 3, renderer-react 104).

**Rivalutazione**: S2 (tipografia per fascia, `fontSize`) può ora procedere senza ulteriori decisioni di classificazione — la stessa categoria `STYLE_KEYS` la copre già. Se emergerà un bisogno concreto di applicare lo stesso valore a più fasce in un solo gesto (segnalato, non richiesto) — da valutare allora, non prima. Il pattern di visibilità condizionale del `PropertyPanel` è ora lo standard per `href` (Fase 9) e per qualunque futuro campo specifico-per-tipo.

---

## D-026 — Tipografia per fascia (S2): campo `fontSize` nel `PropertyPanel`, visibile solo sui tipi che portano testo

**Stato**: nuovo campo `fontSize` (`TextField`, stringa grezza — mirror di `color`, nessun editor strutturato a 3 campi) nel `PropertyPanel`, visibile solo quando `isTextBearingType(node.type)` è vero (nuovo modulo puro `elements/textBearingTypes.ts`, elenco `["text","h1","h2","h3","paragraph","link"]` — gli stessi e soli tipi che hanno `fontSize` nei propri default, `elements/createElementCommand.ts`). A differenza della condizione per `columns`/`gap` (S1, `resolvedProps.layoutMode`, un valore CHE PUÒ avere un override per fascia), la condizione qui controlla `node.type` diretto — `type` non ha mai un override responsive, nessun comando lo modifica dopo `CREATE_NODE` (verificato), quindi non c'è nulla da risolvere.

`TextField` (componente condiviso con `text`/`color`) esteso con un `badge` opzionale (mirror di `NumberField`, che già lo aveva) — usato per `fontSize` (mostra ereditato/overridden-here via `frozenFieldState`, D-025), non usato per `text`/`color` (CONTENT_KEYS, nessun concetto di "per fascia" per loro, D-018).

**Motivazione — nessuna nuova decisione architetturale**: la classificazione (`STYLE_KEYS`) e il pattern di visibilità condizionale erano già stati decisi esplicitamente in S1/D-025, prima che questa fase iniziasse. `[Fatto verificato: test/write/buildUpdatePropsCommand.test.ts, blocco "'fontSize' accettato come STYLE_KEYS", scritto in S1]` il comportamento di congelamento per `fontSize` era già testato e verde PRIMA di iniziare questa fase — S2 non ha toccato l'Engine, il Resolver, né `buildUpdatePropsCommand.ts`: è stata quasi interamente un'aggiunta di UI (un campo, una condizione, un piccolo modulo puro).

**Motivazione — stringa grezza, non un editor strutturato**: stessa scelta già fatta in D-023 per lo stesso motivo (nessuna specifica di prodotto per un assemblatore min/preferred/max, nessuna evidenza che serva ora) — costruirlo qui avrebbe ripetuto una decisione già presa senza un fatto nuovo che la giustifichi.

**Evidenza disponibile**: `test/elements/textBearingTypes.test.ts` (2 test — i 6 tipi che portano testo, esclusione di tutti gli altri incluso quelli preesistenti). `test/panel/frozenFieldState.test.ts` (+1: stesso comportamento generico già dimostrato per `columns`, ora su `fontSize`). Verificato in un browser reale: un nodo non testuale (`"box"`) non mostra `fontSize`; un `"text"` e un `"h1"` (Fase 9) lo mostrano entrambi, col default `clamp(16px, 2vw, 24px)`; modificare `fontSize` su `mobile-verticale` produce badge "overridden-here" lì E su `tablet-verticale` (congelato al valore risolto pre-modifica) mentre `laptop-compatto` resta "inherited" e invariato — stessa identica verifica già fatta per `columns` in S1. Zero errori console. Suite complete del monorepo verdi (Engine 191, CLI 20, Test Runner 3, renderer-react 107).

**Rivalutazione**: se in futuro servirà un editor strutturato (min/preferred/max) per `fontSize` — stessa nota di rivalutazione già in D-023, non anticipata qui. Se emergerà un altro campo specifico-per-tipo (es. `href`, Fase 9) — segue lo stesso pattern ormai consolidato (S1 → S2 → prossimo): un piccolo modulo puro `type → bool`/condizione sui `resolvedProps`, un campo condizionale nel pannello.

---

## D-027 — SEO per pagina (Fase 14): `Page.props` bag libero senza cascata per fascia, nucleo `title`/`description`/`canonical` (Fase 14)

**Stato**: `Page` porta ora un campo `props: Readonly<Record<string, unknown>>` (`packages/engine/src/document/types.ts`), bag libero mirror di `DocumentNode.props` nella RAPPRESENTAZIONE (stesso meccanismo di estensione incrementale già usato 10+ volte per i nodi, mai un cambio di tipo) ma non nel COMPORTAMENTO: `Page.props` non passa mai dal Resolver, nessuna cascata per fascia — un titolo SEO non varia guardando la stessa pagina a una larghezza diversa (Punto 1 dell'analisi, decisione esplicita del proprietario del prodotto: Opzione B). Aggiunto senza bump di `schemaVersion` (resta 2): campo opzionale, additivo, stesso precedente già stabilito per `pageOrder` (Fase 5, Blocco A) — contrapposto esplicitamente al rename dei breakpoint (D-019), che invece bump la versione perché breaking.

Nuovo comando `UPDATE_PAGE_PROPS` (`runtime/commands.ts`): merge shallow su `page.props`, stessa semantica di `UPDATE_PROPS` per i nodi. `deserializeDocument`/`hashDocument` estesi simmetricamente (`parsePropsEntries` generalizzato con un `ownerLabel: "Node" | "Page"`, `serializePage` ora include `props` negli entries ordinati).

Nucleo (Punto 2, decisione esplicita): SOLO `title`, `description`, `canonical`. Nessun campo `og:*`, né per-pagina né per-documento — non richiesti dai dati reali del sito di riferimento per questo nucleo, rimandati.

`IR.meta` (`export/types.ts`) esteso con `pageProps`, passato così com'è (Punto 7): l'Engine non interpreta `title`/`description`/`canonical` per nome — stesso principio già rispettato per `DocumentNode.props` (`text`/`color`/`href`/`fontSize` mai letti per nome nell'Engine). La convenzione dei nomi SEO vive solo in `renderer-react`. Questo rende `Page.props` osservabile end-to-end via `builder export` senza un Exporter HTML reale (fuori scope di questa fase).

`renderer-react`: nuovo `write/buildUpdatePagePropsCommand.ts`, elenco chiuso `PAGE_SEO_KEYS = ["title","description","canonical"]` (stessa disciplina di errore-esplicito-su-chiave-sconosciuta di `GEOMETRY_KEYS`/`STYLE_KEYS`/`CONTENT_KEYS`), ma SENZA alcuna logica di congelamento/cascata (non prende né `document` né `activeBreakpoint`, a differenza di `buildUpdatePropsCommand`) — coerente con la non-cascata decisa al Punto 1. Editing UI in `PageManager.tsx` (Punto 6, decisione esplicita del proprietario del prodotto), non nel `PropertyPanel`: un titolo SEO appartiene alla pagina nel suo insieme, non a un nodo selezionato.

**Motivazione**: tutti e tre i punti lasciati esplicitamente aperti nell'analisi (bag libero vs. campi tipizzati; ambito del nucleo; collocazione dell'editing) sono stati decisi direttamente dal proprietario del prodotto prima dell'implementazione — nessuna decisione presa in autonomia.

**Cross-dipendenza verso Fase 16b (registrazione font)**: il pattern qui validato — bag libero a livello `Page` (o `Document`), mirror rappresentazionale di `DocumentNode.props` ma senza cascata per fascia, con editing in una superficie UI dedicata fuori dal `PropertyPanel` — è il candidato naturale per la futura registrazione font a livello documento (identificato durante l'analisi di dipendenza Fase 14→15→16→17): un font registrato non varia per fascia più di quanto non vari un `<title>`. Non implementato qui, solo la struttura è riusabile.

**Evidenza disponibile**: `test/document/deserialize.test.ts`, `test/document/hash.test.ts`, `test/pages.test.ts`, `test/publicApi.test.ts`, `test/export/exportIR.test.ts` aggiornati per il nuovo campo `props`/`pageProps` — 191 test verdi nell'Engine. `test/write/buildUpdatePagePropsCommand.test.ts` (nuovo, 5 test) verde in renderer-react — 112 test verdi in totale nel pacchetto. CLI (20 test) e Test Runner (3 test) verdi, un'assertion in `test/lifecycle.test.ts` aggiornata per `pageProps`. Verificato end-to-end (Punto 7) con un documento reale: `builder create` → `UPDATE_PAGE_PROPS` con `title`/`description`/`canonical` → `serializeDocument`/`deserializeDocument` round-trip → `builder export` → `meta.pageProps` nell'output JSON contiene esattamente i tre valori scritti. Verificato in un browser reale: i tre campi SEO in `PageManager.tsx` (sotto l'elenco pagine, per la pagina attiva) accettano testo e lo mantengono dopo il blur, zero errori console riconducibili alla modifica.

**Rivalutazione**: se in futuro servirà un campo `og:*` (per-pagina o per-documento) — decisione esplicitamente rimandata al Punto 2, da riaprire quando emergerà un caso reale (es. condivisione social), non anticipata qui. Se `Page.props` dovesse mai aver bisogno di una cascata per fascia — sarebbe una decisione architetturale nuova, non coperta da questa voce.

---

## D-028 — Elemento immagine (Fase 15): `image`→`img`, overlay di selezione ristrutturato per compatibilità con void element, `objectFit` in `STYLE_KEYS`

**Stato**: nuovo `ElementType`/`DocumentNode.type` `"image"` (`elements/createElementCommand.ts`), mappato a `<img>` (`elements/htmlTag.ts`) — primo tag VOID di questo elenco (h1/h2/h3/p/a/div ammettono tutti children). Nessuna modifica a `packages/engine`: `type` resta una stringa libera mai interpretata dall'Engine (stesso fatto già sfruttato in Fase 9), e `computeLayout.ts` posiziona ogni foglia da `x/y/width/height` espliciti senza alcun concetto di dimensione intrinseca — un'immagine è, per l'Engine, un box come un altro.

Default alla creazione: `src` = un placeholder SVG inline (data URI, nessuna chiamata di rete — Punto 2, decisione esplicita: NON una stringa vuota, a differenza di `href` in Fase 9, perché un `<img src="">` nascerebbe visibilmente rotto mentre un `<a href="">` resta comunque testo selezionabile), `alt` = stringa vuota editabile (Punto 3, incluso nel nucleo fin da subito — a differenza di `href`, il sito reale mostra `alt` sempre presente in qualche forma, mai assente), `objectFit` = `"cover"` (Punto 4, terza voce di `STYLE_KEYS` dopo `columns`/`gap`/`fontSize` — il sito reale usa sia `cover` sia `contain` a seconda dell'immagine, nessun default unico li copre entrambi, da qui un campo editabile invece di un valore fisso).

`write/buildUpdatePropsCommand.ts`: `src`/`alt` aggiunti a `CONTENT_KEYS` (nessuna evidenza di sorgente diversa per fascia, stesso ragionamento già accettato per `href` in D-024 — non una decisione aperta), `objectFit` aggiunto a `STYLE_KEYS` (stesso congelamento di `fontSize`). `PropertyPanel`: tre campi condizionali su `node.type === "image"`, stesso pattern di visibilità condizionale di S1/S2/D-025/D-026.

**Punto 1 (bug strutturale, non di prodotto) — overlay di selezione ristrutturato**: prima di questa fase, il pulsante "Sposta dentro…" e la maniglia di ridimensionamento in `Canvas.tsx` erano renderizzati come FIGLI del tag selezionato, sfruttando il fatto che ogni tag esistente (`div`/`h1`/`h2`/`h3`/`p`/`a`) ammette children. `<img>` è un void element — React genera un errore/warning se gli si passano children reali. Corretto con l'Opzione A dell'analisi (decisione esplicita del proprietario del prodotto): l'overlay è ora un FRATELLO del tag, non un figlio, posizionato con le stesse coordinate locali `x`/`y`/`width`/`height` già calcolate in `renderBox` invece che relativamente al tag genitore. Generalizza per qualunque tag void presente o futuro, nessuna eccezione per tipo.

**Regressione trovata e corretta durante la verifica in browser (non dai test unitari — nessuno copre `Canvas.tsx`)**: la maniglia di ridimensionamento aveva `stopPropagation` solo sul suo `onPointerDown`, mai sul `click` risultante. Da figlio, quel click risaliva comunque attraverso l'`onClick` del tag genitore, che lo assorbiva riselezionando innocuamente lo stesso nodo. Da fratello, il tag genitore non è più un antenato: il click risaliva indisturbato fino al contenitore del Canvas, che deseleziona al click sullo sfondo — ogni ridimensionamento deselezionava immediatamente il nodo appena ridimensionato. Corretto aggiungendo lo stesso `onClick`+`stopPropagation` già presente sul pulsante "Sposta dentro…".

**Motivazione**: tutti e quattro i punti lasciati esplicitamente aperti nell'analisi (overlay su void element; default di `src`; ambito di `alt`; `objectFit`) sono stati decisi direttamente dal proprietario del prodotto prima dell'implementazione.

**Evidenza disponibile**: `test/elements/htmlTag.test.ts` (+1), `test/elements/createElementCommand.test.ts` (+3), `test/write/buildUpdatePropsCommand.test.ts` (+4) — 120 test verdi in renderer-react (era 112). Engine (191), CLI (20), Test Runner (3) invariati e verdi (nessuna modifica all'Engine). Verificato in un browser reale: creazione di un'immagine, overlay di selezione funzionante su `<img>` senza errori console; ridimensionamento e spostamento via drag funzionanti, selezione preservata dopo il ridimensionamento (regressione sopra, verificata corretta); editing di `src`/`alt`/`objectFit` dal `PropertyPanel` riflesso nell'attributo/stile dell'`<img>`; overlay ancora funzionante e posizionato pixel-per-pixel correttamente (bottone 22px sopra il box, maniglia nell'angolo in basso a destra) su un nodo esistente non-void (`box`/testo) dopo la ristrutturazione di `Canvas.tsx` — nessuna regressione sui tipi preesistenti.

**Rivalutazione**: se emergerà un asset manager reale (upload, libreria immagini) — il placeholder inline e il campo `src` testuale libero andranno riconsiderati, non anticipato qui. Se emergerà un bisogno di immagini responsive (sorgente diversa per fascia) — riaprire la classificazione di `src` come `CONTENT_KEYS`, oggi non ambigua per mancanza di evidenza reale.

---

## D-029 — Font custom (Fase 16): `Document.props.fonts`, registrazione via Font Loading API (non `<style>`), `fontFamily`/`fontWeight` in `STYLE_KEYS`

**Stato**: nuovo campo `props: Readonly<Record<string, unknown>>` su `Document` (`packages/engine/src/document/types.ts`), bag libero mirror di `Page.props` (Fase 14) un livello più in alto (Punto 1 - decisione esplicita: Opzione B). Corregge l'inquadramento originale della roadmap 6C ("riuso di `Page.props`" per la registrazione font): un font è condiviso da tutto il documento, non da una pagina - fatto verificato sul sito di riferimento (5 font dichiarati una sola volta, "Oswald" dichiarato e mai usato da nessun elemento di nessuna pagina). Aggiunto senza bump di `schemaVersion` (stesso precedente `pageOrder`/`Page.props`). Nuovo comando `UPDATE_DOCUMENT_PROPS` (mirror di `UPDATE_PAGE_PROPS`, shallow merge diretto, nessuna cascata Resolver). `deserializeDocument`/`hashDocument`/`exportIR` estesi simmetricamente (`IR.meta.documentProps`, stesso principio di passaggio opaco di `pageProps`).

Nucleo (Punto 2, decisione esplicita: Opzione A): un'unica chiave nel bag, `fonts` - un array di `{family, weight, src}` (`renderer-react/src/fonts/fontRegistration.ts`), nessun nuovo campo tipizzato su `Document`. `weight` obbligatorio (Punto 4 - il sito reale usa pesi diversi della stessa famiglia, es. Poppins 500/600, selezionati proprio tramite `font-weight` sull'elemento che la usa: senza un peso esplicito nel nucleo, quella seconda variante resterebbe di fatto non selezionabile in modo affidabile).

`write/buildUpdateDocumentPropsCommand.ts`: `buildRegisterFontCommand`/`buildUnregisterFontCommand`, a differenza di `buildUpdatePagePropsCommand` prendono `document` in input (l'unica chiave del nucleo è un ARRAY: registrare/rimuovere un font significa leggere lo stato corrente e scrivere un array nuovo per intero, `UPDATE_DOCUMENT_PROPS` fa uno shallow merge non un merge di array - stesso bisogno già visto in `buildFrozenResponsive`). Editing UI in un nuovo `fonts/FontManager.tsx` (mirror di `PageManager.tsx`), non nel `PropertyPanel` (una registrazione non è una proprietà di un nodo selezionato) né in `PageManager.tsx` (non è una proprietà di una pagina).

`fontFamily`/`fontWeight` aggiunti a `STYLE_KEYS` (Punto 3/4): STESSA classificazione già data a `fontSize` (D-025) - non decisa per analogia in autonomia, ma confermata esplicitamente dal proprietario del prodotto (a differenza di `href`/`src`/`alt`, la classificazione tipografica non era stata considerata "non ambigua": `fontSize` stesso era stato lasciato esplicitamente aperto in D-023). Campi condizionali nel `PropertyPanel` sugli stessi tipi di `fontSize` (`isTextBearingType`).

**Deviazione dall'analisi - meccanismo di rendering**: l'analisi ipotizzava (linguaggio "probabilmente", non una decisione bloccante) un `<style>` condiviso con `@font-face` costruito per interpolazione di stringa. In implementazione, `family`/`weight`/`src` sono valori scelti dall'utente in campi di testo liberi (stesso principio "nessun asset manager" di Fase 15) - interpolarli in testo CSS grezzo dentro un `<style>` avrebbe aperto una superficie di CSS injection (es. una `family` contenente `';}body{...}` altererebbe regole CSS arbitrarie sulla pagina). Implementato invece con la CSS Font Loading API (`new FontFace(...)` + `document.fonts.add(...)`, `fonts/useRegisteredFonts.ts`): stessi valori passati come DATI (argomenti), non come testo CSS da interpretare - stessa funzionalità approvata ("un font registrato è visivamente renderizzato in Canvas e Preview"), nessuna superficie di interpolazione. Effetto collaterale positivo: `document.fonts` (il registro font del browser) è globale alla pagina, quindi un solo punto di chiamata (`App.tsx`) serve sia Canvas sia Preview, più semplice di quanto ipotizzato nell'analisi (che immaginava un componente condiviso duplicato nei due alberi).

**Motivazione**: tutti e quattro i punti lasciati esplicitamente aperti nell'analisi (scala Document vs. Page; forma dei dati; classificazione `fontFamily`; inclusione di `fontWeight`) sono stati decisi direttamente dal proprietario del prodotto prima dell'implementazione. La sostituzione `<style>`→Font Loading API è un affinamento tecnico nell'esecuzione dello stesso obiettivo già approvato (evita una superficie di CSS injection reale), non una nuova decisione di prodotto - segnalata qui per trasparenza, non richiesta come approvazione separata.

**Evidenza disponibile**: nuovo `test/commandBus.test.ts` (+3, `UPDATE_DOCUMENT_PROPS`) - 194 test verdi nell'Engine (era 191; nessun test dedicato esisteva per `UPDATE_PAGE_PROPS` da Fase 14, gap pre-esistente non toccato da questa fase, segnalato separatamente). `test/fonts/fontRegistration.test.ts` (+4), `test/write/buildUpdateDocumentPropsCommand.test.ts` (+5), `test/write/buildUpdatePropsCommand.test.ts` (+2) - 133 test verdi in renderer-react (era 120). CLI (20) e Test Runner (3) invariati e verdi. Verificato end-to-end via `builder create`/`UPDATE_DOCUMENT_PROPS`/`builder export`: `meta.documentProps.fonts` riflette esattamente il valore scritto. Verificato in un browser reale con un font TTF reale (94KB, non un placeholder finto): registrazione di due font (uno applicato a un elemento testuale, uno mai usato da nessun nodo - stesso caso di "Oswald"), `getComputedStyle().fontFamily` riflette il valore applicato, `document.fonts.check('700 16px "BigShoulders"')` conferma che il font è REALMENTE caricato (non un prop inerte), il font mai usato non produce errori, rimozione di un font funzionante. Zero errori console riconducibili alla fase.

**Rivalutazione**: se emergerà un asset manager reale per i font - il campo `src` testuale libero andrà riconsiderato, stessa nota già in D-028 per le immagini. Se `fontWeight` si rivelerà insufficiente (es. servisse `font-style: italic`) - da valutare allora, non anticipato qui. Se in futuro emergerà davvero un bisogno di font diversi per fascia responsive (Punto 3, oggi non richiesto da alcuna evidenza) - riaprire la classificazione, non prevista come probabile ma nemmeno esclusa esplicitamente dal proprietario del prodotto.

---

## D-030 — Transizioni CSS di base (Fase 17): `props.hover` come quarto bag, `:hover` reale via CSSOM, attivo solo in Preview

**Stato**: nuovo bag `props.hover` (`interactions/hoverStyle.ts`, renderer-react) - mirror STRUTTURALE di `props.responsive` (Fase 6) ma per STATO invece che per fascia, NON integrato con esso (Punto 2 dell'analisi, approvato: nessuna cascata, `:hover` non compare mai dentro un `@media` nel sito di riferimento - fatto verificato). Elenco chiuso guidato dall'evidenza reale: `color`, `background`, `transform`, `borderColor` - esplicitamente escluse geometria/layout (nessuna evidenza reale, avrebbe richiesto un concetto di "stato transitorio" nel Layout Engine che oggi non esiste). `transition` (la proprietà di timing) resta uno scalare separato sui props BASE del nodo, aggiunto a `STYLE_KEYS` (mirror esatto di `fontSize`) - mai dentro `props.hover`, mirror del sito reale (`transition` è sempre sulla regola base, mai dentro `:hover`).

**Punto 1 (decisione di fondo, approvata)**: `:hover` realizzato con CSS reale, non una simulazione JS via `onMouseEnter`/`onMouseLeave`. Implementato con CSSOM (`sheet.insertRule` + assegnazione PROGRAMMATICA delle singole proprietà su `rule.style`, mai interpolazione di testo CSS) - stesso principio di sicurezza già scelto per la Font Loading API in Fase 16 (D-029): `color`/`background`/`transform`/`borderColor` sono campi di testo liberi dell'utente. Selettore `[data-node-id="..."]` (attributo già presente su ogni `Tag` di `Canvas.tsx`, aggiunto ora anche a `Preview.tsx` dove mancava).

**Punto 3 (decisione di fondo, approvata)**: `:hover`/`transition` attivi SOLO in `Preview.tsx`, mai in `Canvas.tsx` - due conflitti tecnici concreti identificati nell'analisi (il trascinamento dal vivo del Canvas, Fase 5 Blocco D, e l'overlay di selezione posizionato su coordinate risolte, Fase 15/D-028) e confermati dal sito reale stesso (`.vc-stage.trascina{transition:none}`, disattivazione esplicita della transizione durante un trascinamento JS). `Canvas.tsx` non è stato toccato in questa fase: non legge mai `resolvedProps.transition`/`.hover`, per costruzione.

**Ambito del `PropertyPanel` — decisione presa dopo un confronto esplicito**: la proposta iniziale (nessuna restrizione di tipo) è stata rivista dopo un fatto verificato più a fondo - nel sito di riferimento OGNI target di una delle 31 regole `:hover` è un `<a>` diretto (30 casi: hero-btn, btn-primario, btn-outline, tutte le `*-btn` per-slide) o il suo contenitore immediato che incapsula un `<a>` (1 caso, `.porta`). I campi hover/transition sono quindi visibili SOLO su `node.type === "link"` (l'unico tipo del sistema che mappa a `<a>`) - non "nessuna restrizione" (nessuna evidenza per testo/immagine/contenitore generico) né "anche i contenitori" (avrebbe riaperto la stessa ambiguità un livello più in alto, dato che "box"/contenitore è il tipo più generico e ubiquo del sistema). L'effetto di sollevamento del contenitore-carta (`.porta`) resta fuori dal nucleo, segnalato, rimandato a un eventuale futuro tipo "card" - stessa logica di rimando già usata per `og:*` (D-027).

**Bug trovato e corretto durante la verifica in browser (non dai test unitari - nessuno rende davvero il DOM)**: il `Tag` renderizzato porta sempre uno style inline (es. `background: backgroundColor ?? "transparent"` in `Preview.tsx`, per il prop `color` del nodo) - uno style inline batte QUALUNQUE regola di un foglio esterno indipendentemente dal selettore, `:hover` incluso (specificità CSS). Senza correzione, l'hover su `background` non aveva mai effetto (verificato: `color`/`transform` funzionavano subito perché Preview.tsx non li imposta mai inline, `background` no). Corretto scrivendo ogni proprietà hover con priorità `important` (`rule.style.setProperty(prop, value, "important")`).

**Segnalazione - fuori dal piano di questa fase, non risolta qui**: verificando il trascinamento nel Canvas su un elemento `link` con un delta ampio (300px), il risultato non rispecchiava il delta atteso (dx=38 invece di 300), mentre lo stesso identico gesto su un `<div>` (`card-2`) produceva un delta esatto (dx=300). Causa isolata: `<a>` è nativamente `draggable` nel browser (`element.draggable === true` di default) - un gesto di trascinamento con puntatore su un `<a>` innesca il drag-and-drop nativo HTML5 (`dragstart` osservato), che intercetta parte della sequenza di eventi prima che il nostro `pointermove` la riceva per intero. **Bug preesistente, latente dalla Fase 9** (introduzione del tipo "link"), non causato da questa fase e non specifico di `transition`/`hover` - riproducibile su QUALUNQUE elemento `link` trascinato nel Canvas, indipendentemente da `props.hover`/`transition`. Non risolto qui (fuori da questo piano); la correzione più probabile è `draggable={false}` sul `Tag` in `Canvas.tsx` (una riga, applicabile uniformemente, innocua per i tag non-`<a>`), da valutare in un momento dedicato.

**Motivazione**: tutti e quattro i punti dell'analisi erano già stati approvati; l'unica decisione presa in questo turno (ambito del `PropertyPanel` ristretto a "link") è stata sottoposta esplicitamente prima dell'implementazione, con l'evidenza a supporto, non decisa in autonomia.

**Evidenza disponibile**: `test/interactions/hoverStyle.test.ts` (+6), `test/write/buildUpdateHoverPropsCommand.test.ts` (+8), `test/write/buildUpdatePropsCommand.test.ts` (+2) - 149 test verdi in renderer-react (era 133). Engine (194), CLI (20), Test Runner (3) invariati e verdi (nessuna modifica all'Engine). Verificato in un browser reale: `style.transition` sul `Tag` del Canvas è sempre stringa vuota anche dopo aver impostato `transition` dal pannello; l'hover su un link nel Canvas non produce alcun cambiamento visibile (`color`/`background`/`transform` prima e durante l'hover identici); nella Preview lo stesso link mostra tutte e tre le proprietà hover correttamente (color rosso, background verde, transform `translateY(-6px)`) al passaggio del mouse; un trascinamento di 300px su un elemento non-link (`card-2`) nel Canvas resta 1:1 anche con `transition` configurata altrove nel documento. Zero errori console riconducibili a questa fase.

**Rivalutazione**: l'effetto di sollevamento del contenitore-carta (`.porta` nel sito reale: un `<div>` che incapsula testo + un `<a class="porta-btn">`, con `transform`/`border-color` sull'hover del `<div>` stesso, non del link interno) resta esplicitamente RIMANDATO - non richiesto da alcun bisogno reale del nucleo, stessa logica di rimando già usata per `og:*` (D-027): da riaprire quando emergerà un tipo "card"/contenitore-interattivo dedicato nel Document Model (oggi non esiste, "box"/contenitore è generico e non distingue "contenitore che incapsula un link" da un contenitore qualunque), non anticipato qui. Se emergerà un bisogno reale di `:active`/`:focus` - da trattare come un'estensione dell'elenco chiuso di `props.hover`/una nuova categoria di stato, non anticipato qui (evidenza reale: 1 sola occorrenza di `:active`, zero `:focus`). Il bug di trascinamento nativo sui link (segnalato sopra) va sistemato in un momento dedicato, non è specifico di questa fase.

---

## D-031 — Fix: `draggable={false}` su ogni `Tag` del Canvas (bug segnalato in D-030, corretto separatamente)

**Stato**: `Canvas.tsx` imposta ora `draggable={false}` incondizionatamente su ogni `Tag` renderizzato. Corregge il bug segnalato in D-030: `<a>` e `<img>` sono nativamente trascinabili nel browser (`element.draggable === true` di default) - un gesto di trascinamento con puntatore su uno di questi tag innescava il drag-and-drop nativo HTML5 (`dragstart`), che intercettava parte della sequenza di eventi prima che il nostro `onPointerDown`/`pointermove` la ricevesse per intero, troncando lo spostamento (verificato: un delta di 300px diventava 38px). Bug preesistente dalla Fase 9 (introduzione del tipo "link"), mai specifico di Fase 17 (hover/transition) - qui corretto come intervento dedicato, non come parte di quella fase, su richiesta esplicita del proprietario del prodotto.

**Motivazione**: applicato incondizionatamente a tutti i tag (non solo `a`/`img`) perché innocuo sui tag già non trascinabili di default (`div`/`h1`/`h2`/`h3`/`p`) - nessuna eccezione per tipo necessaria, nessuna decisione aperta.

**Evidenza disponibile**: verificato in browser che `draggable` non ha alcun effetto collaterale sui comportamenti nativi rilevanti per l'editor: il click su un link selezionato continua a funzionare (nessuna navigazione, il pannello mostra il nodo selezionato - stesso comportamento già garantito da Fase 9, Punto 5, qui riconfermato) e l'overlay di selezione ("Sposta dentro…", Fase 15/D-028) resta visibile e funzionante. Trascinamento riverificato 1:1 esatto sia su un elemento `link` (300px richiesti, 300px ottenuti) sia su un elemento `image` (200px richiesti, 200px ottenuti - `<img>` è ugualmente draggable di default, stesso bug, stessa correzione). Nessun test unitario dedicato: `Canvas.tsx` non ha copertura di test automatizzata (stesso limite già presente per l'intero file, verificato solo in browser fin dalla Fase 5, Blocco D). Suite complete del monorepo verdi (149 test in renderer-react, invariati - nessuna funzionalità nuova, solo una correzione).

**Rivalutazione**: nessuna prevista - la correzione è definitiva per la causa individuata, non introduce un'eccezione o un caso limite da riconsiderare.

---

## D-032 — B1: `href` modificabile — nessuna validazione ora, sanificazione in output vincolante per l'Exporter

**Stato**: `href` entra in `CONTENT_KEYS` (`write/buildUpdatePropsCommand.ts`) — nessuna cascata per fascia (stesso trattamento di `text`/`color`/`src`/`alt`; classificazione già decisa in D-024, non riaperta qui). Campo nel `PropertyPanel`, nessun badge, visibile solo su `node.type === "link"` (stesso ambito di hover/transition, Fase 17). Nessuna modifica al percorso di lettura (Canvas.tsx/Preview.tsx leggono già `props.href`, invariati da D-024).

**Decisione (analisi B1, Opzione A approvata)**: nessuna validazione di schema al momento della scrittura. Il Command Bus valida CHIAVI (elenco chiuso), mai VALORI — principio mai violato in questo file, qui deliberatamente non rotto per `href` nonostante sia il primo campo il cui valore, se mai reso in un output pubblicato, ha significato eseguibile (uno schema `javascript:` in un `href` cliccato da un visitatore reale eseguirebbe codice). Motivazione: il rischio è oggi strutturalmente irraggiungibile — il click su un `<a>` non naviga mai dentro l'editor (`e.preventDefault()`, D-024, invariato), quindi nessun visitatore reale può raggiungere questo valore finché non esiste un Exporter. Costruire una difesa ora, per un canale oggi irraggiungibile, sarebbe difesa prematura — stesso principio già usato per rimandare il Capability Registry completo (D-008): non indovinare una whitelist senza un secondo caso d'uso reale che la vincoli.

**Condizione vincolante (non opzionale, posta esplicitamente dal proprietario del prodotto)**: la gestione sicura di `href` in output è un **criterio di chiusura obbligatorio dell'Exporter**, non di questo punto. Quando la fase Exporter verrà aperta, dovrà definire esplicitamente quali schemi/formati sono ammessi in un `href` pubblicato e impedire che valori come `javascript:` (o altri schemi eseguibili) vengano emessi come `href` nell'HTML statico prodotto. La whitelist precisa NON è decisa qui — verrà definita nell'analisi dell'Exporter stesso, sulla base dei requisiti effettivamente supportati in quella fase (dati reali già raccolti come riferimento: nel sito di riferimento i soli schemi osservati sono ancora interna `#...`, `https://`, `mailto:` — zero `javascript:`, zero `tel:`, zero `target="_blank"`).

**Evidenza disponibile**: `test/write/buildUpdatePropsCommand.test.ts` (+4: nessun errore per `href`, scrittura diretta sui props base indipendentemente dalla fascia attiva, nessuna validazione anche per uno schema eseguibile come `javascript:alert(1)` - verifica esplicita della decisione presa, non solo un'omissione - stringa vuota accettata) — suite aggiornata e verde in renderer-react. Verificato in un browser reale: il campo `href` compare nel `PropertyPanel` solo quando il nodo selezionato è di tipo "link"; un click su un link con un `href` reale non vuoto non naviga (comportamento invariato, `e.preventDefault()` regge anche con un valore non vuoto); il valore scritto sopravvive a un secondo giro di lettura (nessuna eccezione nel round-trip generico di `UPDATE_PROPS`).

**Rivalutazione**: nessuna per la classificazione (chiusa da D-024). La decisione "nessuna validazione ora" va rivista SOLO quando la fase Exporter definirà la whitelist reale - a quel punto si deciderà anche se un controllo lato editor (Opzione B/C della analisi B1) diventi utile in aggiunta alla sanificazione in output, non in sua sostituzione.

---

## D-033 — B2: identificatore stabile per ancore interne — prop `anchorId` separato da `NodeId`, disponibile su tutti i tipi

**Stato**: `anchorId` entra in `CONTENT_KEYS` (`write/buildUpdatePropsCommand.ts`) — nessuna cascata per fascia (stesso ragionamento di `href`, D-032: nessuna evidenza reale di un'ancora diversa per fascia responsive). Campo "ancora" nel `PropertyPanel`, **sempre visibile, nessuna condizione di tipo** — a differenza di ogni altro campo condizionale del pannello (`href`/hover ristretti a "link", `src`/`alt`/`objectFit` a "image", ecc.), qui l'analisi B2 ha verificato che i 13 bersagli reali di un'ancora nel sito di riferimento non condividono un ruolo strutturale comune (mai un "link" tra loro) — nessuna restrizione pulita è supportata dai dati.

**Decisione (analisi B2, Opzione C approvata)**: `anchorId` è un prop LIBERO, separato e MAI sincronizzato con `NodeId` — che resta un campo strutturale del Document Model (usato da `parentId`/`childrenIds`/History/hash), non toccato da alcun comando, invariato. Motivazione: stesso pattern già consolidato tre volte (`Page.props`/D-027, `Document.props`/D-029, `props.hover`/D-030) — il rischio architetturale più basso tra le tre alternative valutate, contro l'esporre l'id tecnico esistente (fragile: si rigenera silenziosamente quando un nodo viene cancellato e ricreato, una volta che B3 esisterà) o permettere la rinomina dell'identità strutturale del nodo (portata sproporzionata, nessun precedente di un comando che tocchi `NodeId` dopo la creazione).

**Nessuna validazione di unicità** (stessa decisione di `href`, D-032): due nodi possono avere lo stesso `anchorId` senza che nulla lo impedisca o lo segnali — coerente col principio che il Command Bus valida CHIAVI, non VALORI.

**Condizione vincolante (non opzionale)**: la garanzia di attributi `id=` HTML univoci nell'output pubblicato è un **criterio di chiusura obbligatorio dell'Exporter**, stessa natura della condizione già registrata per `href` in D-032 — non decisa qui, da definire quando quella fase verrà aperta.

**Rendering**: un vero attributo HTML `id=` (non solo `data-node-id`, che resta invariato e continua a servire drag/overlay/`useHoverStyles`) su Canvas.tsx e Preview.tsx, applicato incondizionatamente su ogni tag quando `anchorId` risolto è una stringa non vuota (una stringa vuota produce l'assenza dell'attributo, non un `id=""` privo di senso). **Nessuna logica di navigazione/scroll-to-anchor aggiunta** (deciso esplicitamente, fuori scope di B2): `onClick` continua a chiamare `e.preventDefault()` su ogni `<a>` sia in Canvas sia in Preview (D-024, invariato) — un click su un link con ancora interna non fa scorrere la pagina all'interno dell'editor, comportamento preesistente e deliberato (Preview è sola lettura, Fase 7/D-020), non toccato da questa fase.

**Evidenza disponibile**: `test/write/buildUpdatePropsCommand.test.ts` (+4: nessun errore per `anchorId` su un nodo di tipo "card" - conferma che l'ambito "tutti i tipi" è una proprietà del Command Bus stesso, non solo del `PropertyPanel` -, scrittura diretta sui props base indipendentemente dalla fascia attiva, nessuna validazione di unicità anche con un valore già usato da un altro nodo, stringa vuota accettata) — suite verde in renderer-react. Verificato in un browser reale: il campo "ancora" compare nel pannello per QUALUNQUE tipo di nodo selezionato (testo, contenitore, immagine, link); un valore scritto produce un vero attributo `id="valore"` sul tag sia nel Canvas sia nella Preview; nessuna eccezione nel round-trip di lettura dopo deselezione/riselezione.

**Rivalutazione**: se l'Exporter (quando aperto) troverà che due `anchorId` duplicati nello stesso documento producono un problema concreto in output — la scelta "nessuna validazione qui" andrà eseguita lì, non qui (stesso principio di D-032). Se in futuro emergerà un bisogno reale di scroll-to-anchor dentro l'editor stesso (oggi nessuna richiesta, deliberatamente fuori scope) — va trattato come un'estensione separata, non un'omissione di questa fase.

---

## D-034 — B3: cancellazione elementi da UI — pulsante in toolbar globale + scorciatoia da tastiera, nessuna conferma

**Stato**: `DELETE_NODE` (già esistente e testato nell'Engine, mai collegato a una UI) è ora raggiungibile da `App.tsx` in due punti di ingresso: un pulsante "Elimina" nella toolbar globale (stesso pattern di Undo/Redo/Anteprima, `disabled` quando non applicabile) e una scorciatoia da tastiera (`Delete` principale, `Backspace` secondario, entrambi con lo stesso comportamento). Nessuna conferma (Opzione D dell'analisi B3, approvata) — coerente con l'unico precedente diretto del prodotto, `DELETE_PAGE` in `PageManager.tsx`, che cancella immediatamente senza dialogo.

**Radice pagina esclusa in entrambi i punti di ingresso**: nuova funzione `isPageRoot` (`App.tsx`, locale, non un'estensione dell'API pubblica dell'Engine) che verifica se il nodo selezionato è la radice di QUALUNQUE pagina del documento — stesso giro già fatto da `applyDeleteNode` nell'Engine (`packages/engine/src/runtime/commands.ts`), non solo della pagina attiva, per coprire anche il caso limite di una selezione residua da una pagina diversa. Stesso principio già usato in `Canvas.tsx` per nascondere "Sposta dentro…" sulla radice (`entry.parentBox !== null`), qui riscritto alla granularità disponibile in `App.tsx` (nessun `FlatBoxEntry` lì).

**Scorciatoia da tastiera — guardie esplicite**: non si attiva quando il fuoco (`document.activeElement`, verificato via `e.target`) è su un elemento editabile (`INPUT`/`TEXTAREA`/`SELECT`/`isContentEditable`) - senza questo controllo, `Backspace` cancellerebbe l'elemento selezionato mentre l'autore scrive in un campo del pannello, non un carattere. Disattivata anche mentre la Preview è aperta: `Preview.tsx` ha già un proprio `onKeyDown` scoped (frecce/Esc per la navigazione) - un listener globale attivo in parallelo lì rischierebbe una cancellazione accidentale durante la sola navigazione, senza alcun indizio visivo (la Preview non mostra selezione) che un nodo resti comunque cancellabile. Il pulsante in toolbar, al contrario, resta sempre disponibile durante la Preview (stesso comportamento già esistente di Undo/Redo, mai disabilitati lì) - solo la scorciatoia è ristretta, non l'azione stessa.

**Deselezione esplicita dopo la cancellazione**: `store.deselect()` chiamato subito dopo `DELETE_NODE`, in entrambi i punti di ingresso - la selezione non viene MAI lasciata "pendente" come esito di un'azione voluta (quel messaggio nel `PropertyPanel` resta per i casi inattesi, es. Undo di una `CREATE_NODE`).

**Cancellazione a cascata**: comportamento ereditato dall'Engine, non una scelta di questa fase - cancellare un contenitore rimuove l'intero sottoalbero (tutti i discendenti), senza alcuna opzione di "cancella solo il genitore, mantieni i figli". Nessun controllo di integrità referenziale con `href`/`anchorId` (B1/B2): un nodo cancellato che era bersaglio di un'ancora o referenziato da un link lascia quel riferimento pendente, senza alcun avviso - coerente con "nessuna validazione ora" già scelto per `href`/`anchorId` (D-032/D-033), segnalato nell'analisi come possibile estensione futura, non un obbligo di questa fase.

**Evidenza disponibile**: nessun nuovo file di test unitario - `App.tsx` non ha copertura di test automatizzata (stesso limite già presente per `Canvas.tsx`/`Preview.tsx`, verificato solo in browser fin dalla Fase 5). Suite complete del monorepo invariate e verdi (Engine 194, CLI 20, Test Runner 3, renderer-react 157 - nessuna modifica ai pacchetti testati). Verificato in un browser reale: "Elimina" disabilitato senza selezione e con la radice pagina selezionata; abilitato e funzionante con un elemento normale selezionato, sia dal pulsante sia da `Delete` da tastiera, in entrambi i casi con deselezione immediata del pannello; `Backspace` premuto dentro il campo "text" del pannello NON cancella l'elemento selezionato; `Delete` premuto con la radice pagina selezionata non ha alcun effetto; un contenitore con un figlio annidato (via "Sposta dentro…") cancellato dalla toolbar rimuove ENTRAMBI, e Undo li ripristina entrambi. Zero errori console riconducibili a questa fase.

**Rivalutazione**: se in futuro emergerà un bisogno reale di "cancella solo il genitore, mantieni i figli" (oggi nessuna richiesta, comportamento ereditato dall'Engine) - andrebbe trattato come una nuova primitiva dell'Engine, non un'estensione di questa UI. Se emergerà un controllo di integrità referenziale più ampio per `href`/`anchorId` - da valutare insieme a quando si deciderà la validazione di quei due punti (D-032/D-033), non isolatamente qui.

---

## D-035 — B4: SEO og:*/lang — ultimo dei quattro bloccanti, collocazione Page.props/Document.props, og:url derivato

**Stato**: `PAGE_SEO_KEYS` (`write/buildUpdatePagePropsCommand.ts`) esteso da `title`/`description`/`canonical` a includere `ogTitle`/`ogDescription` — stesso trattamento, nessuna cascata per fascia, editabili in `PageManager.tsx` (sezione "SEO — {pagina}"). Nuova funzione `buildUpdateDocumentSeoCommand` (`write/buildUpdateDocumentPropsCommand.ts`), elenco chiuso `DOCUMENT_SEO_KEYS = ["lang", "ogSiteName", "ogType", "ogLocale"]` — **primo pattern scalare in quel file**, mirror diretto di `buildUpdatePagePropsCommand` (shallow merge, nessuna lettura di `document` necessaria), a differenza delle funzioni esistenti per `fonts` (semantica ad array, "leggi tutto-scrivi tutto"). Nuovo componente `seo/SiteSeoManager.tsx` (mirror di `FontManager.tsx`/della sezione SEO di `PageManager.tsx`), montato in `App.tsx` accanto a `FontManager`.

**Ampiezza (Opzione B, analisi approvata)**: copertura completa dell'evidenza reale — tutti e 6 i campi `og:*`/`lang` osservati nel sito di riferimento sono rappresentabili, non solo il sottoinsieme "univoco" (Opzione A, scartata): `og:type`/`og:locale` hanno un solo valore osservato nel caso reale ma sono comunque dati REALMENTE presenti, non inventati.

**Collocazione finale** (decisione esplicita, non dedotta dall'analisi da sola): `Document.props` → `lang`, `ogSiteName`, `ogType`, `ogLocale` (un solo sito, un solo valore, nessuna struttura per-pagina indovinata senza evidenza — anche se lo standard Open Graph li definirebbe nominalmente per-URL, l'unico valore osservabile nel caso reale non lo giustifica). `Page.props` → `ogTitle`, `ogDescription` (accanto a `title`/`description`/`canonical` già esistenti — l'Open Graph li definisce per-URL, stesso livello semantico dei campi già presenti).

**`og:url` — Opzione H (derivato), nessun campo nel Document Model**: `og:url` non è mai scrivibile, né in `Page.props` né in `Document.props` — deriverà sempre da `canonical` (già presente) nel punto in cui un futuro Exporter lo genererà. Motivazione: un `og:url` scritto a mano duplicherebbe un dato già presente senza alcun controllo che li tenga sincronizzati — primo caso nel prodotto in cui due campi avrebbero dovuto sempre coincidere, evitato scrivendone uno solo. **Verificato esplicitamente, non lasciato all'assenza silenziosa**: un test dedicato (`test/write/buildUpdatePagePropsCommand.test.ts`, `test/write/buildUpdateDocumentPropsCommand.test.ts`) dimostra che l'elenco chiuso RIFIUTA esplicitamente una chiave `ogUrl` in entrambi i punti di scrittura; un secondo test dedicato a livello Engine (`test/export/exportIR.test.ts`, non solo il test generale su `ir.meta`) dimostra che `canonical` scritto su `Page.props` arriva in `ir.meta.pageProps` così com'è, e che nessuna chiave `ogUrl`/`og:url` compare MAI né in `ir.meta.pageProps` né in `ir.meta.documentProps`, nemmeno dopo aver scritto ogni altro campo SEO per-pagina e a livello documento.

**Costo reale riconosciuto esplicitamente**: `buildUpdateDocumentPropsCommand.ts` non aveva, prima di questa fase, un pattern generico per chiavi scalari (solo la logica dedicata ad array di `fonts`) — `buildUpdateDocumentSeoCommand` è un'aggiunta reale a quel file, non un'estensione a costo zero, anche se il modello da copiare (`buildUpdatePagePropsCommand.ts`) esisteva già pronto.

**Nessuna validazione** (coerente con B1/B2, D-032/D-033): nessun controllo di forma su nessuno dei nuovi campi.

**Evidenza disponibile**: `test/write/buildUpdatePagePropsCommand.test.ts` (+4: ogTitle/ogDescription accettati e scritti, rifiuto esplicito di `ogUrl`), `test/write/buildUpdateDocumentPropsCommand.test.ts` (+6: nuovo describe per `buildUpdateDocumentSeoCommand`, elenco chiuso, rifiuto esplicito di `ogUrl`, scrittura diretta) — 167 test verdi in renderer-react (era 157). `packages/engine/test/export/exportIR.test.ts` (+3: canonical→pageProps, nessun ogUrl in pageProps, nessun ogUrl in documentProps) — 197 test verdi nell'Engine (era 194). CLI (20) e Test Runner (3) invariati e verdi. Verificato end-to-end: `createDocument` → `UPDATE_PAGE_PROPS`/`UPDATE_DOCUMENT_PROPS` con tutti i nuovi campi → `serializeDocument` → `builder export` (CLI reale, non solo l'API dell'Engine) → l'IR risultante contiene esattamente i valori scritti in `meta.pageProps`/`meta.documentProps`, senza alcuna chiave `ogUrl`/`og:url` in nessuno dei due bag. Verificato in un browser reale: i campi `og:title`/`og:description` compaiono nella sezione SEO esistente di `PageManager.tsx`; la nuova sezione "SEO — sito" con `lang`/`og:site_name`/`og:type`/`og:locale` compare nella barra laterale; tutti i valori scritti persistono dopo il blur. Zero errori console riconducibili a questa fase.

**Chiusura dei quattro bloccanti**: con B4, tutti e quattro i gap bloccanti identificati nell'analisi "requisiti minimi di un sito pubblicabile" (B1 href, B2 identificatore per ancore, B3 cancellazione elementi, B4 SEO og:*/lang) sono chiusi. Il builder ha ora tutte le primitive di dato necessarie perché un futuro Exporter possa rappresentare correttamente ciò che l'editor permette di costruire, per il caso reale di riferimento — l'Exporter stesso resta l'unico lavoro che manca prima di un sito realmente pubblicabile (nessuna modifica a quel fronte in questa fase).

**Rivalutazione**: se un futuro Exporter troverà che `og:type`/`og:locale` devono variare per pagina in un caso reale diverso da questo (oggi nessuna evidenza) — la collocazione in `Document.props` andrebbe riaperta, spostando quei due campi in `Page.props` (stesso costo di implementazione, solo un file diverso). Se emergerà un caso reale in cui `og:url` deve differire da `canonical` — la decisione "sempre derivato" andrebbe riaperta esplicitamente, non assunta.

---

## D-036 — Exporter Batch 1: `IR.nodes` (nodeId → resolvedProps), Opzione B dell'analisi "come estendere l'IR"

**Stato**: `IR` esteso con un nuovo campo `nodes: Readonly<Record<NodeId, Readonly<Record<string, unknown>>>>` (`packages/engine/src/export/types.ts`), SIBLING di `box`/`meta`, non annidato dentro `meta`. `exportIR.ts` cammina il `Box` già calcolato (non l'intero `ResolvedModel` multi-pagina) per raccogliere i `nodeId` della sola pagina esportata, e per ciascuno scrive `resolvedNode.resolvedProps` — **mai** un `DocumentNode`/`ResolvedNode` intero (niente `id`/`type`/`parentId`/`childrenIds`). `IR.box`, `Box`, `computeLayout`, il Layout Engine restano interamente invariati — nessuna modifica al contratto geometrico (RFC-004).

**Bug trovato e corretto, dentro il perimetro esplicito di questo batch** (non un'estensione di scope: "struttura dei resolvedProps deterministica" era già un requisito dichiarato): un test di determinismo preesistente (`exportIR.test.ts`, mai toccato prima da questo batch) è fallito appena `IR.nodes` è entrato nel confronto byte-per-byte. Causa: `resolvedProps` è costruito da `UPDATE_PROPS` via merge incrementale (`{...vecchie, ...nuove}`) - l'ordine delle chiavi dell'oggetto JS risultante riflette l'ordine con cui le proprietà sono state scritte nel tempo, non il loro contenuto logico. Finché `resolvedProps` non entrava in alcun output serializzato (mai prima di oggi), questo era invisibile. Stesso problema già risolto una volta per `Page.props`/`Document.props`/`node.props` in `document/hash.ts` (`sortedEntries`) - qui corretto con lo stesso principio (una nuova funzione locale `sortedProps` in `exportIR.ts`, shape diversa: un oggetto piatto normale, non un array di coppie, per l'ergonomia dei consumer che si aspettano `ir.nodes[nodeId].chiave` come accesso diretto). Fix interamente contenuto in `exportIR.ts` - Resolver, Canvas, Preview invariati.

**Impatto sui consumer esistenti**: **zero modifiche** ad alcun test o file consumer. I due `toEqual` stretti che l'analisi precedente prevedeva di dover aggiornare meccanicamente (`publicApi.test.ts`, `test-runner/lifecycle.test.ts`) non hanno richiesto alcuna modifica, perché entrambi controllano `ir.meta` isolatamente (non l'intero `ir`) — la collocazione di `nodes` come sibling di `meta`, non al suo interno (decisione dell'analisi precedente), si è rivelata ancora più a basso impatto del previsto. Tutti i test CLI (`exportCommand.test.ts`/`export.test.ts`/`roundtrip.test.ts`) restano verdi senza modifiche, per costruzione (confrontano `exportIR()` con se stesso).

**Verifica del commit `de2dae9` menzionato**: cercato in reflog, in tutti i branch locali e remoti (incluso l'altro branch remoto presente nel repository, relativo a un lavoro non correlato), e per messaggio di commit in tutta la storia (`git log --all --grep`) — **non trovato in nessuna forma**. Repository verificato pulito e allineato a `origin` (`3536bd7`, B4) prima di iniziare. Batch 1 implementato da zero su quella base, nessun reset o cancellazione necessari.

**Evidenza disponibile**: `test/export/exportIR.test.ts` (+7, nuovo describe dedicato: resolvedProps vs DocumentNode, nodo senza props, `IR.box` invariato via confronto diretto con `computeLayout(resolveDocument(...))`, scoping alla sola pagina esportata su un documento multi-pagina, ordine deterministico dei `nodeId`, ordine deterministico delle chiavi di `resolvedProps` indipendente dall'ordine dei comandi, determinismo byte-per-byte su chiamate consecutive) — 204 test verdi nell'Engine (era 197). CLI (20) e Test Runner (3) invariati e verdi senza alcuna modifica ai loro file. Verificato end-to-end con la CLI reale (`builder export`): l'IR prodotto ha `box`/`nodes`/`meta` come chiavi di primo livello, `nodes` contiene esattamente i `resolvedProps` per `nodeId`, ordinati.

**Rivalutazione**: nessuna prevista per questo batch — chiude esattamente il perimetro dato. Il fix del determinismo di `resolvedProps` (`sortedProps`) è locale a `exportIR.ts`; se in futuro `resolvedProps` dovesse essere serializzato altrove con lo stesso bisogno di determinismo, valutare se estrarre `sortedProps` in un'utility condivisa invece di duplicarla.

