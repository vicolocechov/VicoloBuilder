{
  "name": "vicolobuilder",
  "private": true,
  "version": "0.0.0",
  "workspaces": [
    "packages/engine",
    "packages/render-conventions",
    "packages/cli",
    "packages/test-runner",
    "packages/renderer-react",
    "packages/exporter"
  ],
  "scripts": {
    "test": "npm run test --workspaces --if-present",
    "build": "npm run build --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
